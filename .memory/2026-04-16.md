## 20:21 [MCP-9] 校审批注处理流分析

- 完成：梳理了当前三维校审中批注评论、流转动作与评论线程的现状，确认已有回复能力但缺少结构化“已修改/不需解决/同意/驳回”状态模型。
- 决策：建议把批注处理状态建模为独立于自由评论的结构化字段，避免继续复用普通评论文本承载处理结果，便于列表筛选、节点权限控制和对话视图并存。
- 待办：若进入实现，可优先扩展 `AnnotationComment` 或新增批注处理实体，并在 `ReviewCommentsTimeline` 与 `ReviewPanel` 的批注列表中增加状态条和节点动作入口。

## 20:34 [MCP-9] 批注处理状态前端闭环

- 完成：按两阶段方案在前端补齐了批注处理状态与动作历史，支持设计侧“已修改/不需解决”和校对审核侧“同意/驳回”，并与原评论时间线合并展示。
- 决策：优先把入口放进 `ReviewCommentsTimeline`，使 `AnnotationPanel` 与 `ReviewPanel` 复用同一套处理 UI，避免两套实现分叉。
- 待办：后续可在后端补 annotation-level status/action 接口与 WebSocket 事件，把当前前端状态持久化并支持多人同步。