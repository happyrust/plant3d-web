# 领域词汇表

## 尺寸与测量

**尺寸标注**:
锚定到几何对象、用于表达工程尺寸关系及其版式的图形化陈述。它可由用户创建或由外部领域提供，但不等同于执行测量后得到的测量结果。
_Avoid_: 测量、measurement

**用户尺寸**:
由用户在校审过程中创建并拥有、可编辑且随工作流记录持久化的尺寸标注。
_Avoid_: MBD 尺寸、测量结果

**外部尺寸**:
由 MBD 等外部工程来源提供、在当前系统中只读且随来源重新加载的尺寸标注。它不会被复制进用户尺寸文档。
_Avoid_: 用户尺寸

**外部标注图元**:
由 MBD 等外部来源提供、经尺寸系统共享呈现内核只读渲染的非尺寸图形元素，例如标签、引线、辅助线/弧/圆/点/文字、焊缝符号和坡度符号。它不是尺寸标注，不进入用户尺寸文档，只随来源整体替换、选择或临时隐藏。
_Avoid_: 外部尺寸、MBD 尺寸

**尺寸标注系统**:
围绕尺寸标注的创建、编辑、呈现和生命周期形成的独立业务能力。它不拥有测量结果、文字批注、云线批注等其他领域记录。
_Avoid_: 通用标注系统、测量系统

**尺寸锚点**:
尺寸标注所引用的稳定几何位置，由设计坐标快照和可选的语义锚点组成，例如 P-Point、实例原点、圆心、圆弧或模型表面点。
_Avoid_: 裸坐标、拾取点

**失效尺寸**:
一个或多个语义锚点在当前模型版本中无法解析的尺寸标注。存在最后一次设计坐标快照时以 `STALE` 标签降级显示；没有可信设计坐标时只出现在列表中。它只允许重新绑定或删除。
_Avoid_: 正常尺寸、自动删除的尺寸

**近似尺寸**:
至少包含一个近似锚点的有效尺寸标注。它的数值仍随当前模型求值，但标签和详情必须明确表示其精度低于由 P-Point、圆心等精确锚点形成的尺寸。
_Avoid_: 精确尺寸、失效尺寸

**线性尺寸**:
表达两个几何锚点之间真实直线距离的尺寸标注。
_Avoid_: 距离测量

**投影线性尺寸**:
表达两个几何锚点之差在指定轴或方向上投影长度绝对值的尺寸标注；带符号的高差或坐标差属于测量结果。
_Avoid_: 水平尺寸、垂直尺寸

**投影轴**:
投影线性尺寸使用的稳定 Design Space 方向，可来自 X/Y/Z 或可重解析的边、管轴、工作平面轴。它不随相机或屏幕方向变化。
_Avoid_: 屏幕水平、屏幕垂直

**角度尺寸**:
表达两条有向几何射线之间夹角的尺寸标注，可明确选择较小弧或较大弧。
_Avoid_: 角度测量

**径向尺寸**:
表达圆或圆弧半径或直径的尺寸标注；半径和直径是同一类尺寸的两种显示语义。
_Avoid_: 圆尺寸

**设计坐标**:
以米为单位、尚未应用查看器重心平移或场景变换的稳定工程坐标。尺寸及测量的持久位置均以设计坐标表达。
_Avoid_: worldPos、localPos

**尺寸放置意图**:
用户对尺寸平面、投影方向、偏移距离、文字沿线位置及内外侧的视图无关选择。屏幕像素微调和自动避让结果不属于放置意图。
_Avoid_: 屏幕坐标、布局缓存

**尺寸值**:
由尺寸类型及其锚点几何派生的工程量，长度以米、角度以弧度表达。它不等同于用户最终看到的格式化文字。
_Avoid_: 标签文字、textOverride

**尺寸标签**:
尺寸值经统一单位、精度、符号、前后缀及公差策略格式化后的显示文字。只有外部只读来源可以声明自己的标签为权威文字。
_Avoid_: 尺寸值

**三维尺寸呈现**:
尺寸线、箭头和矢量文字作为视口场景图元呈现；图元面向观察者、保持稳定的屏幕视觉尺度并默认置于模型之前，但不属于模型实体。
_Avoid_: 二维尺寸覆盖层、物理标牌、模型几何

**尺寸显示平面**:
由设计空间锚点和当前相机共同确定的临时观察平面；相机变化时可以重算，不进入尺寸文档。
_Avoid_: 持久化标注平面、固定世界平面

**显示避让偏移**:
为减少当前视口中文字重叠而计算的临时位移；不改变测量语义，也不写回尺寸文档。
_Avoid_: 标签位置、设计偏移

**工程文字朝向**:
尺寸值沿尺寸线方向排布并自动翻转到可读方向；设备编号、坐标和多行说明保持视口水平。
_Avoid_: 全水平文字、物理表面贴字

**三维布局结果**:
尺寸内核产出的权威场景图元，包括三维线段、箭头、圆弧和 LFF 字形；二维几何只能由其投影得到。
_Avoid_: 屏幕布局结果、Canvas 图元

**标签固定**:
用户拖拽标签后持久化的布局状态；固定标签保留米制语义位置，不参与自动避让，直到用户恢复自动布局。
_Avoid_: 临时显示避让、锁定测量锚点

**测量结果尺寸**:
现有测量工具或管道间距工具产生的距离、角度或高程结果；作为统一三维画家的只读输入呈现，不再拥有独立逐对象渲染类。
_Avoid_: 普通标注、尺寸文档记录

**版本尺寸批次**:
版本对比中按一个具体模型版本的数据和锚点独立生成的三维尺寸批次；只在对应版本的 render pass 中呈现。
_Avoid_: 共享尺寸批次、复制当前版本几何

**参考尺寸**:
由外部工程来源明确标记为非控制性、并以 `REF` 呈现的尺寸标注。用户创建的尺寸不能切换为参考尺寸。
_Avoid_: 虚线尺寸、用户参考开关

**语义锚点**:
尺寸锚点中可选的模型语义引用（构件、点源、候选点），用于在模型版本变化后重新解析锚点位置；渲染不依赖它。
_Avoid_: entityId

## E3D 操作模式

面向熟悉 AVEVA E3D 的用户提供的测量交互口径。它强调对象捕捉过滤、清晰的捕捉提示、连续测量和测量结果上下文操作，而不是复制完整 E3D 建模或定位系统。

## Object Snap Filter

测量命令中的捕捉过滤器。用户通过它限定哪些点源可以成为测量点。显示候选点和允许捕捉是两个不同概念。

## P-Point

来自模型点集数据的设计关键点，适合作为精确测量点。它不是模型表面任意射线命中点。

## 实例原点

模型实例自身变换定义的原点。它不等同于 E3D Item 端点、几何中心或 P-Point。

## 模型表面点

鼠标射线命中的模型表面位置。它适合作为自由点或近似测量点，在 E3D 操作模式中不应默认伪装成精确捕捉点。

## 尺寸放置态

尺寸创建过程中锚点已锁定、但尺寸线位置与投影方向尚未落定的中间态；期间尺寸随光标实时预览，确认后才成为尺寸标注记录。

## 测量草稿

用户已锁定部分测量点、但尚未完成保存的临时测量状态。

## 连续测量

完成一次测量后，用户可以用上一次结果继续下一次测量，减少重复启动命令的动作。

## 模型版本查看

**最小交付单元模型版本**:
同一最小交付单元在指定数据库和 sesno 下的只读模型状态，由 `(dbnum, unit_refno, sesno)` 唯一标识。
_Avoid_: 发布版本、release、model_version_id

**模型版本对**:
同一最小交付单元的两个模型版本，其中 A 是较早的基准、B 是较新的目标；跨数据库、跨最小交付单元或 A 不早于 B 的组合不是有效版本对。
_Avoid_: 发布对、跨单元对比

**无几何变化提交**:
sesno 已推进但模型生成相关属性未变化的模型版本；它保留版本轨迹并复用较早模型资产。
_Avoid_: 空版本、缺失模型

**已删除单元版本**:
最小交付单元在指定 sesno 已不存在的只读状态；版本视图以明确的删除空态显示，而不是将它解释为模型加载失败。
_Avoid_: 空模型、缺失模型、加载失败

**最新环境模型**:
打开版本对比时作为空间上下文固定下来的当前 dbnum 汇总模型；它不参与版本选择，并隐藏正在对比的最小交付单元以避免重复几何。一次对比会话内保持不变，显式刷新环境后才由新的最新汇总模型替换。
_Avoid_: 环境版本、历史环境模型

**单视口版本切换**:
在一个最新环境模型中，将版本 A 和版本 B 保持在各自设计坐标，并且任一时刻只显示用户当前选择的一个版本。
_Avoid_: 同时叠加显示、移动后对比

**双视口分屏版本视图**:
在两个并列视口中分别显示版本 A 和版本 B；两侧使用同一口径的最新环境模型和一致的观察方向，版本模型保持各自设计坐标。
_Avoid_: 在单个场景中左右移动模型、两个无关查看器

**模型几何差异**:
两个模型版本在 refno 粒度上的新增、删除或修改；模型资产引用相同的无几何变化提交不产生模型几何差异。
_Avoid_: 任意属性差异、sesno 变化本身

## 模型按需生成

**显式显示操作**:
用户明确要求让某个模型范围的几何可见的操作。可见性控制和“显示模型”命令属于显式显示操作；仅选中、搜索定位、查看属性或展开树节点不属于显式显示操作。
_Avoid_: 选择模型、访问模型

**按需模型生成**:
显式显示操作所请求的模型资产尚不可用时，为满足本次显示意图而启动的异步模型生产过程。它不由普通选择或导航动作隐式触发。
_Avoid_: 模型加载、重新生成模型

**共享生成任务**:
代表一个确定模型生成范围的单一活动任务。同一范围已有排队中或运行中的共享生成任务时，后续请求关注并复用该任务的进度和结果，而不重复创建生成工作。
_Avoid_: 用户私有生成任务、重复生成任务

**模型生成范围**:
为满足对一个根模型节点的显式显示意图而必须共同产出的完整模型集合。它由根节点和统一的领域展开规则确定，不等同于调用方临时拼出的任意 refno 列表。
_Avoid_: 单个 refno、前端 refno 列表

**生成预检**:
在创建生成工作前，对目标模型资产是否可用、规范生成范围和预计生成成本进行的无副作用评估。预检结果决定请求可直接显示、可直接排队，还是必须先取得用户确认。
_Avoid_: 模型生成、模型加载

**需确认生成**:
生成预检依据统一成本策略判定为影响较大的按需模型生成；只有用户明确确认后才能进入任务队列。
_Avoid_: 所有缺失模型、前端数量阈值

**上下文生成进度**:
围绕触发按需模型生成的模型范围就地呈现的非阻塞任务状态。它帮助用户继续当前操作并查看所关注任务的排队和执行进展，但不替代集中展示全部任务的任务监控。
_Avoid_: 阻塞式生成弹窗、全局唯一任务进度

**显示意图**:
用户当前希望某个模型范围在查看器中可见的临时意图。它由显式显示操作建立，在用户隐藏该范围或离开对应项目上下文时失效；生成任务完成后，只有仍有效的显示意图才会触发自动加载。
_Avoid_: 选中状态、任务所有权

**生成任务关注**:
某个前端会话对共享生成任务进度和结果的临时关注关系。撤销关注只停止该会话的上下文反馈和完成后自动加载，不取消共享生成任务本身。
_Avoid_: 任务所有权、取消生成任务

**可用模型资产**:
与目标模型当前源数据修订和生成配置相匹配、可以直接加载的模型产物。无几何变化时可以沿明确的资产沿袭关系复用较早产物；仅存在任意历史文件不代表资产可用。
_Avoid_: 已有模型文件、任意缓存模型

**资产就绪**:
共享生成任务已经产出可用模型资产的完成状态。资产就绪表示后端生产工作完成，但不表示任何具体浏览器已经把该资产加入查看器场景。
_Avoid_: 模型已显示、模型已加载

**模型加载**:
浏览器读取可用模型资产、解析并将其加入当前查看器场景的本地过程。它可以紧接资产就绪发生，但不属于共享生成任务的进度。
_Avoid_: 模型生成、资产就绪

**交互生成任务**:
由当前显式显示意图发起的共享生成任务。它在受配额约束的前提下获得高于普通批处理的交互优先级，但不会无限抢占或饿死其他生成工作。
_Avoid_: 永久最高优先级任务、用户私有任务

**模型资产补齐**:
为具有模型查看权限的用户确保目标当前可用模型资产存在的幂等操作。资产已可用时不创建生成工作；资产缺失时可以发起按需模型生成。
_Avoid_: 强制重新生成、管理员重建

**模型重新生成**:
即使已有可用模型资产，也明确要求重新生产并替换它的受控操作。它与面向普通查看者的模型资产补齐具有不同的权限和风险。
_Avoid_: 按需模型生成、模型资产补齐
