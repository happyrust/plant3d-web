# 领域词汇表

## 尺寸与测量

**尺寸标注**:
锚定到几何对象、用于表达工程尺寸关系及其版式的图形化陈述。它可由用户创建或由外部领域提供，但不等同于执行测量后得到的测量结果。
_Avoid_: 测量、measurement

**用户尺寸**:
由用户在校审过程中创建并拥有、可编辑且随工作流记录持久化的尺寸标注。
_Avoid_: MBD 尺寸、测量结果

**外部尺寸**:
由 MBD 等外部工程来源提供、在当前系统中只读且随来源重新加载的尺寸标注。它不会被复制进用户尺寸文档。
_Avoid_: 用户尺寸

**外部标注图元**:
由 MBD 等外部来源提供、经尺寸系统共享呈现内核只读渲染的非尺寸图形元素，例如标签、引线、辅助线/弧/圆/点/文字、焊缝符号和坡度符号。它不是尺寸标注，不进入用户尺寸文档，只随来源整体替换、选择或临时隐藏。MBD 的标签与它的引线配成一条记录，默认按屏幕定尺的 billboard（坐标卡片 + 圆点 / 位号方框 / 标高药丸——弯头、弯管、沿管标高变化处与分支点的 `PE`）呈现：标签体大小不随相机变，锚点与引线终点留在管上的三维点，标签体按确定顺序让开尺寸数字、其它标签、尺寸描边、宿主交出的构件包围盒（投影凸包）与视口覆盖层（坐标 gizmo 的屏幕矩形）——先取零侵入的候选位，没有则取加权侵入最少者，不为躲障碍出屏；文字与位置语义仍是来源的（ADR 0058 / 0059，`mbd_3d=0` 回到原位平面文字）。
_Avoid_: 外部尺寸、MBD 尺寸

**尺寸标注系统**:
围绕尺寸标注的创建、编辑、呈现和生命周期形成的独立业务能力。它不拥有测量结果、文字批注、云线批注等其他领域记录。
_Avoid_: 通用标注系统、测量系统

**尺寸锚点**:
尺寸标注所引用的稳定几何位置，由设计坐标快照和可选的语义锚点组成，例如 P-Point、实例原点、圆心、圆弧或模型表面点。
_Avoid_: 裸坐标、拾取点

**失效尺寸**:
一个或多个语义锚点在当前模型版本中无法解析的尺寸标注。存在最后一次设计坐标快照时以 `STALE` 标签降级显示；没有可信设计坐标时只出现在列表中。它只允许重新绑定或删除。
_Avoid_: 正常尺寸、自动删除的尺寸

**近似尺寸**:
至少包含一个近似锚点的有效尺寸标注。它的数值仍随当前模型求值，但标签和详情必须明确表示其精度低于由 P-Point、圆心等精确锚点形成的尺寸。
_Avoid_: 精确尺寸、失效尺寸

**线性尺寸**:
表达两个几何锚点之间真实直线距离的尺寸标注。
_Avoid_: 距离测量

**投影线性尺寸**:
表达两个几何锚点之差在指定轴或方向上投影长度绝对值的尺寸标注；带符号的高差或坐标差属于测量结果。
_Avoid_: 水平尺寸、垂直尺寸

**投影轴**:
投影线性尺寸使用的稳定 Design Space 方向，可来自 X/Y/Z 或可重解析的边、管轴、工作平面轴。它不随相机或屏幕方向变化。
_Avoid_: 屏幕水平、屏幕垂直

**角度尺寸**:
表达两条有向几何射线之间夹角的尺寸标注，可明确选择较小弧或较大弧。
_Avoid_: 角度测量

**径向尺寸**:
表达圆或圆弧半径或直径的尺寸标注；半径和直径是同一类尺寸的两种显示语义。
_Avoid_: 圆尺寸

**设计坐标**:
以米为单位、尚未应用查看器重心平移或场景变换的稳定工程坐标。尺寸及测量的持久位置均以设计坐标表达。
_Avoid_: worldPos、localPos

**尺寸放置意图**:
用户对尺寸平面、投影方向、偏移距离、文字沿线位置及内外侧的视图无关选择。屏幕像素微调和自动避让结果不属于放置意图。
_Avoid_: 屏幕坐标、布局缓存

**尺寸值**:
由尺寸类型及其锚点几何派生的工程量，长度以米、角度以弧度表达。它不等同于用户最终看到的格式化文字。
_Avoid_: 标签文字、textOverride

**尺寸标签**:
尺寸值经统一单位、精度、符号、前后缀及公差策略格式化后的显示文字。只有外部只读来源可以声明自己的标签为权威文字。
_Avoid_: 尺寸值

**三维尺寸呈现**:
尺寸线、箭头和矢量文字作为视口场景图元呈现；图元面向观察者、保持稳定的屏幕视觉尺度并默认置于模型之前，但不属于模型实体。外部来源（MBD）显式给出的箭头笔画例外：以来源几何为准 1:1 呈现，只设一个像素可读下限——投影过短时绕尖端在屏幕上拉伸，不替换成屏幕定尺箭头（ADR 0056）。MBD 长度尺寸（running dimension）再例外一层：默认按**三维标注**呈现——尺寸线沿求解器 `dim_dir` 以字高为单位离开管体，尺寸界线从管表面引出并超出尺寸线，实心箭头与数字落在包含尺寸线、面向相机的三维平面里随透视缩放，数字在线上方而非中断处；求解器的数值、分段、行归属与沿线位置不变（ADR 0057，`mbd_3d=0` 回到原位平面呈现）。
_Avoid_: 二维尺寸覆盖层、物理标牌、模型几何

**尺寸显示平面**:
由设计空间锚点和当前相机共同确定的临时观察平面；相机变化时可以重算，不进入尺寸文档。
_Avoid_: 持久化标注平面、固定世界平面

**显示避让偏移**:
为减少当前视口中文字重叠而计算的临时位移；不改变测量语义，也不写回尺寸文档。
_Avoid_: 标签位置、设计偏移

**工程文字朝向**:
尺寸值沿尺寸线方向排布并自动翻转到可读方向；设备编号、坐标和多行说明保持视口水平。
_Avoid_: 全水平文字、物理表面贴字

**三维布局结果**:
尺寸内核产出的权威场景图元，包括三维线段、箭头、圆弧和 LFF 字形；二维几何只能由其投影得到。
_Avoid_: 屏幕布局结果、Canvas 图元

**标签固定**:
用户拖拽标签后持久化的布局状态；固定标签保留米制语义位置，不参与自动避让，直到用户恢复自动布局。
_Avoid_: 临时显示避让、锁定测量锚点

**测量结果尺寸**:
现有测量工具或管道间距工具产生的距离、角度或高程结果；作为统一三维画家的只读输入呈现，不再拥有独立逐对象渲染类。
_Avoid_: 普通标注、尺寸文档记录

**版本尺寸批次**:
版本对比中按一个具体模型版本的数据和锚点独立生成的三维尺寸批次；只在对应版本的 render pass 中呈现。
_Avoid_: 共享尺寸批次、复制当前版本几何

**参考尺寸**:
由外部工程来源明确标记为非控制性、并以 `REF` 呈现的尺寸标注。用户创建的尺寸不能切换为参考尺寸。
_Avoid_: 虚线尺寸、用户参考开关

**语义锚点**:
尺寸锚点中可选的模型语义引用（构件、点源、候选点），用于在模型版本变化后重新解析锚点位置；渲染不依赖它。
_Avoid_: entityId

## E3D 操作模式

面向熟悉 AVEVA E3D 的用户提供的测量交互口径。它强调对象捕捉过滤、清晰的捕捉提示、连续测量和测量结果上下文操作，而不是复制完整 E3D 建模或定位系统。

## Object Snap Filter

测量命令中的捕捉过滤器。用户通过它限定哪些点源可以成为测量点。显示候选点和允许捕捉是两个不同概念。在 E3D 操作模式下它就是拾取层的拾取过滤器那一维。

## 拾取层

测量取点共用的两维选择：**拾取过滤器**（Any / Element / Aid / Pline / Ppoint / Screen / Graphics / External，决定哪一类特征可以被拾中）× **拾取类型**（Snap / Distance / Mid-Point / Fraction / Proportion / Intersect / Cursor，决定在拾中的点、线、面上落点怎么派生），外加 Significant Snaps 一档；两维正交。Any 只放行 Element、Ppoint、Pline 与模型表面点，不放行 Graphics 细节；Graphics 的边与面从已加载网格派生，属于近似点；Intersect 由两（三）次子拾取的线 / 面求交得到一个位置（ADR 0060）。管身轴线是元素类拾取落在直管上时拾中的线（E3D TUBING），由直管的放置矩阵派生、两端吸到邻接 P-Point，只在 Any 与 Element 下参与。设计点随 P-Point 在 Any 与 Ppoint 下参与；设计辅助只在 Aid 下参与。
_Avoid_: 点源开关、捕捉模式

## 设计辅助

用户在本会话里画出来、专供测量取点用的辅助几何，对应 E3D 的 Design Aid（`GPHLINE` / `GPHPLANE`，`!!aidNumbers` 里的一号）。只有两类：**辅助线**（有限的起点 → 终点）与**辅助面**（位置 + 法向，画成一张 x × y 的矩形框加一根法向短线，缺省 5000 × 5000 mm）。它只在拾取过滤器选 Aid 时能被拾中（E3D `stdAid`；Any 从不拾 aid）：线上任意处可拾，Snap 取近端、Cursor 取线上离光标最近处、Mid-Point 等沿线派生；面取射线与面的交点，且射线要落在画出的矩形上。它当 Perpendicular to 的目标时是无限线 / 无限面（E3D `getLine()` / `getPlane()` 的 DESIGNAID 分支），当 Intersect 的操作数时交出 LINE / PLANE；目标名就是辅助自己的名字（描述，或缺省的 `Line [n]` / `Plane [n]`）。会话级、只在内存、按创建顺序编号，刷新即丢——E3D 的 aid 同样不落库。面只给法向时面内 Y 取 Up 的投影（水平面取 North）、X = Y × Z，与 E3D 水平面缺省一致，倾斜面未对 E3D 核对（决策 d-561，golden MD §28）。
_Avoid_: 测量标注 / 尺寸（测出来的结果，不是拾取对象）、Aid POSITION / ARC / grid（不做）、把辅助放进 Any 过滤器、持久化辅助

## 设计点

设计元素（EQUI / SUBE / STRU / FRMW / TMPL …）名下设计点集 `DPSE` 里的 `DPCA` / `DPCY`，各带 `NUMB` / `POS` / `ORI`，在属主上表现为 `DPPS[n]`（世界位置 = 属主 `world_transform` × POS）与 `DPDI[n]`（世界方向 = ORI 的 Z 轴，ORI 按 Rz · Ry · Rx 合成）——对应 E3D 拾取类型 `DPOINT`。它随目录 P-Point 一起被拾：拾取过滤器 Any / Ppoint 放行，Element / Pline / Graphics / Aid 不放行；P-Point 正压在设计点上时 P-Point 赢。Snap / Cursor / Mid-Point 等回它本身，Distance 沿 `DPDI` 偏移，Intersect 当过该点、沿 `DPDI` 的线，Perpendicular to 取**过该点、法向 = `DPDI` 的面**（E3D `getLine()` 没有 DPOINT 分支、`getPlane()` 给 `Z is dpdir`）。光标悬停到有几何的叶子时沿属主链向上（到 ZONE / SITE 之下）逐个属主查一次 DPSE，悬停过的属主才有候选（与 P-Point 同一口径）。DPCY 按 DPCA 处理（决策 d-563，golden MD §29）。
_Avoid_: P-Point（目录点，另一类）、把 `DPDI` 当轴线给 Perpendicular to、DDSE / DDCA（设计数据集，不是点）

## 管身轴线

直管（TUBI）对象的中心线段，作为一条线参与测量取点：Snap 取近端、Cursor 取轴线上离光标最近处、Mid-Point 等沿线派生、Intersect 当直线。它从画出来的直管对象的局部包围盒与放置矩阵派生，两端再吸到邻接构件的 P-Point，对应 E3D `EDGTUBING.line` 的 leave → arrive 口径；ATTA 是穿过点——管线越过它到下一个非 ATTA 构件，在 ATTA 处断开的两段直管算同一条轴线；OLET 这类非 ATTA 的零长构件不穿过。
_Avoid_: 管道中心线（MBD 尺寸的走向线）、PLINE

## 元素轴线

元素类拾取拾中一个元素本身、而这一击要的是一条线（Intersect 的操作数、Perpendicular to 的目标）时，元素代表的那条线：P-Point 1 → P-Point 2，对应 E3D `edgTypes.attribute(noun).line(item)`；只有圆柱、锥 / 斗、碟、棱锥、管嘴有这条线，弯头 / 盒 / 阀没有（拒收）。它只当操作数——Snap / Mid-Point 拾这些元素仍落元素原点。目录件的 P1 / P2 来自点集；gen-model 的设计基本体没有点集，就从它画在自己局部帧里的几何（局部包围盒 × 放置矩阵）取两端面中心，截面不对中（带 XOFF / YOFF）时不取。
_Avoid_: 管身轴线（直管才有）、显著点、P-Point 方向线（单个 P-Point 带方向的那条）

## p-line

型材（SCTN / GENSEC）截面上带名字的纵向线（NA / TOS / BOS / LTOS …），来自目录 SPRF 的 PSTR 里每条 PLIN 的截面内坐标，随截面一起被 JUSL 对齐、LMIRR 镜像、BANG 旋转后沿轴从起端面拉到终端面——对应 E3D 的 `PLSTART pline → PLEND pline`。在测量取点里它是 Pline 过滤器拾中的那条线：光标落在梁上任意处就拾中整条 p-line，Snap 取近端、Mid-Point 等沿线派生、Intersect 当直线、Perpendicular to 以它为目标。gen-model-v1 下由服务端 `element/plines` 沿建型材实体的同一条截面链算出（所以线一定在画出来的梁上），legacy 下来自模型包导出的 PLINE 端点。斜切端面的端点（`PLSTCUT / PLENCUT`）另给，默认不用——E3D Pick Settings「Pline End Position = Cut」时整条线换成它们。同一设置里的 **Significant Snap Points**（Fittings / Joints / Nodes）把型材名下的 FITT / SJOI+SUBJ / SNOD 投到 p-line 上当分段点（服务端 `snap_points` 给成员位置），Significant snaps 开着时 Snap / Mid-Point 等只在光标所在那一段上做；缺省全关（E3D `EDGPLINE` 构造缺省），整条线不分段。
_Avoid_: PLINE 关键点（把它当两个孤立端点）、管身轴线、元素轴线（型材的「轴线」就是它的 NA p-line）

## 测量显示单位

距离测量这一会话里结果表与尺寸文字共用的长度格式，由 Unit type（Default / Metric）× Display Unit（Millimetres / Centimetres / Metres）两档选出，对应 E3D Measure Distance 窗体的 Units 框与 `COMFORMATS.distanceFormat`。它是**会话级**的，优先级高于全局单位设置——只有 Default 档才回落到全局（E3D 那边回落的是工程当前的 `!!distanceFmt`）。各档的小数位与去不去尾零由格式定（毫米 2 位去尾零、厘米 / 米 3 位去尾零），四舍五入到 0 一律不带负号。Display Unit 记得住上次选的那一档，切回 Metric 时回到它。E3D 还有一档 Imperial（Inch / Feet & Inches / Feet），本项目有意不做。
_Avoid_: 全局单位设置（模型显示单位 + 小数位，另一层）、模型单位

## 三点角

按 E3D 的 root / first / second 三击测出的夹角（Web 草稿里依次是顶点、第一边点、第二边点）。它只报 minor 角（0°–180°，没有反角），两条臂各有一个单位方向（Direction1 = 顶点→第一边点，Direction2 = 顶点→第二边点），方向随当前 wrt 帧换分量与轴标签，角度本身与参考系无关。三点共线（0° / 180°）或有重合点时**测不出来**：不落记录、回到第 1 步重新取顶点——对应 E3D 那边 `radius3PointsNoError` 回未设 ARC、窗体报 `An angular dimension could not be constructed from the data selected`。
_Avoid_: 反角 / 优角、两线夹角（两条拾中的线 / 线与面之间的角，另一种入口）

## 两线夹角

按 E3D「Angle 2 Lines」两击测出的夹角：第一击拾一条线（Graphics 边、p-line、轴线），第二击拾一条线或一个面，对应 E3D `gmfArc.radius2Lines` 出的 ARC，结果表与三点角同一张（Decimal Angle / DMS / Direction1 / Direction2）。弧心是两线的交点，两线异面时取**第一条线**上离第二条最近的点；两条臂都从弧心朝用户拾中的那一侧，所以报的是两条拾中的半线的夹角，点另一侧得补角（E3D 行为）。线与面：参照是线在面上的投影——线在面内是 0° 弧，线垂直于面是 90°，否则弧心是线穿过面的点。两线平行、线平行于面但不在面内、几何退化都**测不出来**：不落记录、丢掉第一条线回第 1 步，提示与三点角退化同一句 E3D 错误。角度入口在结果卡的 `Angle` 下拉里切（Angle 3 Points / Angle 2 Lines），记住上次选的；两击都不算草稿。夹角小于 0.01° 视为平行（float32 网格边的 Web 容差）。
_Avoid_: 三点角（三击、有顶点）、Perpendicular to（点到线 / 面的距离，不是角）、Intersect 拾取类型（同样两击线 / 面，但出的是一个点）

## 角度显示单位

三点角这一会话里结果表用的角度格式，由 Unit（Default / Degrees / Radians / Gradians，Default 就是度）× Decimal Places（0–8，缺省 2，越界或非数字打回 2）两档选出，对应 E3D Measure Angle 窗体的 Units 框。它与距离那边的**测量显示单位**是两套、互不影响（E3D 也是两张窗体）。小数位同时管角度值与两条**罗盘方向串**里的角度，但尾零规则不同：角度值去尾零、方向留尾零。结果表四行 `Decimal Angle`（数值 + 空格 + 单位词）/ `DMS`（恒按十进制度截断出度分秒，与 Unit 无关）/ `Direction1` / `Direction2`。
_Avoid_: 测量显示单位（距离那一套）、全局小数位

## 罗盘方向串

测量结果表里表达一个方向的字串，照 E3D `DIRECTION.string()` 的写法：`W 11.7755 N 66.8266 D` = 在水平面里从 W 朝 N 转 11.7755°、再朝下倾 66.8266°。主轴取水平两分量里绝对值大的那个（第一个角总在 0–45°，打平时 E / W 在前），水平正落在轴上省掉「角 + 次轴字母」（`N 78.6901 U`），在水平面里省掉倾角（`S`），纯竖直只剩 `U` / `D`，零向量是 `--`。数字是 PML REAL 的缺省字串——6 位有效数字、去尾零，不是固定几位小数。字母指的是当前 wrt 帧的三根轴（轴 1 → E/W、轴 2 → N/S、轴 3 → U/D），换帧字母不变、值变。三张表都**不带** ` WRT …` 尾巴——E3D 的标准距离表带、垂距表与角度表不带，Web 统一不带（用户拍板，d-515）：wrt 在结果卡顶上另有一格。
_Avoid_: 分量串（`X +0.36 · Y -0.39 · Z -0.85`，2026-09-14 前的旧写法）、带 `WRT` 尾巴的串、方向角、方位角

## GENSEC 截面标架

GENSEC 当 wrt 时距离结果表三个 Offset 所沿的三根轴：Z = 型材轴向（SPINE 方向）、Y = 截面 Y（BANG 转过之后）、X = Y × Z，即 E3D `gphdimension.offsetType()` 里的 `ORIENTATION('Z IS zDir AND Y IS yDir')`——与元素的 ORI 帧不是一回事（G3-04 两者差 90°）。GENSEC 帧下三个 Offset 是差向量在这三根轴上的**非负**投影（E3D 取 `from` 到各轴线垂足的距离），单元格不带符号；Direction 则例外地按 World 出、字母不跟帧；轴标签仍是 U/V/W。Web 没有 `yDir` / `zDir` 属性，从 gen-model-v1 `element/plines` 每条 p-line 的截面内偏移 + 世界起点反解（`gensecSectionBasis.ts`），解不出（含弧 SPINE、legacy 源、接口失败）就回落 ORI 帧的轴上取绝对值；元素类型只是提示，查不到当普通元素帧。其它元素帧不受影响：仍是 ORI 帧的带符号分量 + 帧字母的罗盘串（决策 d-529，golden MD §26）。
_Avoid_: 拿 GENSEC 的 ORI 当截面标架、拿 `element/ptset` 的 `world_transform`（属主链矩阵）当 yDir / zDir、给 GENSEC 帧的 Offset 加正负号、对 ORI 分量取绝对值当「非负投影」、GENSEC 帧下按 U/V/W 字母出 Direction

## P-Point

来自模型点集数据的设计关键点，适合作为精确测量点。它不是模型表面任意射线命中点。

## 实例原点

模型实例自身变换定义的原点。它不等同于 E3D Item 端点、几何中心或 P-Point。

## 模型表面点

鼠标射线命中的模型表面位置。它适合作为自由点或近似测量点，在 E3D 操作模式中不应默认伪装成精确捕捉点。

## 尺寸放置态

尺寸创建过程中锚点已锁定、但尺寸线位置与投影方向尚未落定的中间态；期间尺寸随光标实时预览，确认后才成为尺寸标注记录。

## 测量草稿

用户已锁定部分测量点、但尚未完成保存的临时测量状态。

## 连续测量

完成一次测量后，用户可以用上一次结果继续下一次测量，减少重复启动命令的动作。

## 模型版本查看

**最小交付单元模型版本**:
同一最小交付单元在指定数据库和 sesno 下的只读模型状态，由 `(dbnum, unit_refno, sesno)` 唯一标识。
_Avoid_: 发布版本、release、model_version_id

**模型版本对**:
同一最小交付单元的两个模型版本，其中 A 是较早的基准、B 是较新的目标；跨数据库、跨最小交付单元或 A 不早于 B 的组合不是有效版本对。
_Avoid_: 发布对、跨单元对比

**无几何变化提交**:
sesno 已推进但模型生成相关属性未变化的模型版本；它保留版本轨迹并复用较早模型资产。
_Avoid_: 空版本、缺失模型

**已删除单元版本**:
最小交付单元在指定 sesno 已不存在的只读状态；版本视图以明确的删除空态显示，而不是将它解释为模型加载失败。
_Avoid_: 空模型、缺失模型、加载失败

**最新环境模型**:
打开版本对比时作为空间上下文固定下来的当前 dbnum 汇总模型；它不参与版本选择，并隐藏正在对比的最小交付单元以避免重复几何。一次对比会话内保持不变，显式刷新环境后才由新的最新汇总模型替换。
_Avoid_: 环境版本、历史环境模型

**单视口版本切换**:
在一个最新环境模型中，将版本 A 和版本 B 保持在各自设计坐标，并且任一时刻只显示用户当前选择的一个版本。
_Avoid_: 同时叠加显示、移动后对比

**双视口分屏版本视图**:
在两个并列视口中分别显示版本 A 和版本 B；两侧使用同一口径的最新环境模型和一致的观察方向，版本模型保持各自设计坐标。
_Avoid_: 在单个场景中左右移动模型、两个无关查看器

**模型几何差异**:
两个模型版本在 refno 粒度上的新增、删除或修改；模型资产引用相同的无几何变化提交不产生模型几何差异。
_Avoid_: 任意属性差异、sesno 变化本身

## 模型按需生成

**显式显示操作**:
用户明确要求让某个模型范围的几何可见的操作。可见性控制和“显示模型”命令属于显式显示操作；仅选中、搜索定位、查看属性或展开树节点不属于显式显示操作。
_Avoid_: 选择模型、访问模型

**按需模型生成**:
显式显示操作所请求的模型资产尚不可用时，为满足本次显示意图而启动的异步模型生产过程。它不由普通选择或导航动作隐式触发。
_Avoid_: 模型加载、重新生成模型

**共享生成任务**:
代表一个确定模型生成范围的单一活动任务。同一范围已有排队中或运行中的共享生成任务时，后续请求关注并复用该任务的进度和结果，而不重复创建生成工作。
_Avoid_: 用户私有生成任务、重复生成任务

**模型生成范围**:
为满足对一个根模型节点的显式显示意图而必须共同产出的完整模型集合。它由根节点和统一的领域展开规则确定，不等同于调用方临时拼出的任意 refno 列表。
_Avoid_: 单个 refno、前端 refno 列表

**生成预检**:
在创建生成工作前，对目标模型资产是否可用、规范生成范围和预计生成成本进行的无副作用评估。预检结果决定请求可直接显示、可直接排队，还是必须先取得用户确认。
_Avoid_: 模型生成、模型加载

**需确认生成**:
生成预检依据统一成本策略判定为影响较大的按需模型生成；只有用户明确确认后才能进入任务队列。
_Avoid_: 所有缺失模型、前端数量阈值

**上下文生成进度**:
围绕触发按需模型生成的模型范围就地呈现的非阻塞任务状态。它帮助用户继续当前操作并查看所关注任务的排队和执行进展，但不替代集中展示全部任务的任务监控。
_Avoid_: 阻塞式生成弹窗、全局唯一任务进度

**显示意图**:
用户当前希望某个模型范围在查看器中可见的临时意图。它由显式显示操作建立，在用户隐藏该范围或离开对应项目上下文时失效；生成任务完成后，只有仍有效的显示意图才会触发自动加载。
_Avoid_: 选中状态、任务所有权

**生成任务关注**:
某个前端会话对共享生成任务进度和结果的临时关注关系。撤销关注只停止该会话的上下文反馈和完成后自动加载，不取消共享生成任务本身。
_Avoid_: 任务所有权、取消生成任务

**可用模型资产**:
与目标模型当前源数据修订和生成配置相匹配、可以直接加载的模型产物。无几何变化时可以沿明确的资产沿袭关系复用较早产物；仅存在任意历史文件不代表资产可用。
_Avoid_: 已有模型文件、任意缓存模型

**资产就绪**:
共享生成任务已经产出可用模型资产的完成状态。资产就绪表示后端生产工作完成，但不表示任何具体浏览器已经把该资产加入查看器场景。
_Avoid_: 模型已显示、模型已加载

**模型加载**:
浏览器读取可用模型资产、解析并将其加入当前查看器场景的本地过程。它可以紧接资产就绪发生，但不属于共享生成任务的进度。
_Avoid_: 模型生成、资产就绪

**交互生成任务**:
由当前显式显示意图发起的共享生成任务。它在受配额约束的前提下获得高于普通批处理的交互优先级，但不会无限抢占或饿死其他生成工作。
_Avoid_: 永久最高优先级任务、用户私有任务

**模型资产补齐**:
为具有模型查看权限的用户确保目标当前可用模型资产存在的幂等操作。资产已可用时不创建生成工作；资产缺失时可以发起按需模型生成。
_Avoid_: 强制重新生成、管理员重建

**模型重新生成**:
即使已有可用模型资产，也明确要求重新生产并替换它的受控操作。它与面向普通查看者的模型资产补齐具有不同的权限和风险。
_Avoid_: 按需模型生成、模型资产补齐

## 模型数据源

**模型数据源 (Model Source)**:
模型树、三维几何、网格与构件属性四条取数链共同指向的那一个后端口径；一个页面同一时刻只有一个，由 `?model_source=` → `VITE_MODEL_SOURCE` → 默认 `gen-model-v1`（2026-09-09 起；此前默认 `legacy`）决定。两个种类：`legacy`（旧后端 `:3100` + parquet / DuckDB-WASM，开关保留一个发布周期）与 `gen-model-v1`（gen-model `/api/v1`）。它是页面级开关，不是某次加载的参数。
_Avoid_: `data_source`（那是 legacy 内部 parquet | backend 的选择）、后端地址、数据库

**数据源端口 (Model Source Port)**:
取数点与数据源之间的四个接口：`TreeSource`（根 / 子节点 / 祖先 / 搜索 / 子树 / 可见实例）、`ModelRecordSource`（`refno → InstanceEntry[]`）、`MeshSource`（`geo_hash → 网格 URL`：legacy 给 `.glb`，gen-model-v1 给 `.mesh` rkyv 直连，DTX 加载链按后缀选解析器）、`AttributeSource`（属性面板 / 类型）。接口形状等于 legacy 函数的形状，所以 legacy 适配器是零逻辑委托；gen-model-v1 适配器负责把 `EleTreeNode` / `GeomInstQuery` / `element/attributes` 映射成这些形状。测量参考系（wrt）的元素帧也跟随当前数据源，但没进这四个接口：`legacy` 走 `:3100 /api/pdms/transform`，`gen-model-v1` 取 `element/ptset` 的 `world_transform`（同是 `get_world_mat4` 的局部→世界矩阵），owner / 元素类型取树节点（d-533）。
_Avoid_: API 客户端、fetch 封装、拿 `element/attributes` 的 stored POS / ORI 自己拼元素帧（那是相对属主的）

**虚拟根 (Virtual Root)**:
gen-model-v1 源下模型树合成的唯一根 `gm-root:<project>:<mdb>`，children 是该 MDB 全部 DESI / ISOD 库的 SITE。它不是 refno，不能 ensure、不能整体显示；祖先链以它为终点。id 里不含 `/` `,` `<` `>`——树的 refno 归一化会改写这些字符。
_Avoid_: WORL、树根 refno

**生成根投影 (Generation-Root Projection)**:
gen-model 的几何记录（`model/records`）按生成根组织：记录上的 `owner` 是生成根而不是直接属主，一个构件的几何要经它所在的根整体取回。前端因此「按根收、按构件缓存」——对任一构件 ensure，服务端解到根、整根记录写进缓存，同根其它构件直接命中。一次 ensure 解出多根时 `records` **多根打包**取（`generation_roots[]`，同 Ref0 的根一批 ≤64，平铺响应按 `owner` 归根；spec §4.5.2，2026-09-10 起），旧服务端不认识该字段就自动退回逐根——批量只是传输形态，缓存与分型口径不变。
_Avoid_: 直接属主、按 refno 单条查询、parquet 分桶、逐根一次请求当成契约

**即时生成数据 (On-Demand Data)**:
gen-model-v1 源下前端拿到的树与三维模型**全部是 API 即时给出的**：树读 `/tree/*`（内存里的 MDB 骨架），几何走 `model/ensure`（当场生成）→ `model/records` → `/meshes/{hash}.mesh`。前端不依赖服务端库里的持久数据、不看 `model-memory` / `model-database` 之分、不订阅 `model_drain` / WS 任务事件、不做被动重载——要新几何就再显示一次或「重新生成」。原「模型变更同步」（WS + 15 s 轮询 `GET /tasks?kind=model_drain` → reload）2026-09-09 按用户口径整条下线（收口计划 §12，决策见共享决策库）。
_Avoid_: 模型变更同步、被动重载、库侧水位、等 drain

**整库入口 (Whole-Dbnum Entry)**:
`show_dbnum` 整库显示在 gen-model 读透 / kv-mem 形态下的取数路（spec §4.5.3，收口计划 §17，2026-09-10 起）：`POST dbnums/{dbnum}/model/ensure`（服务端自己枚举该库全部生成根、202 回 `task_id` + 真数 `expected_roots`、e3d-model 流水线并行生成、按片提交进进程内投影）→ 每拍只查**自己刚发起的这一个** `task_id`（进度）+ `GET dbnums/{dbnum}/model/roots?ready=1`（哪些根的投影已提交）→ 新就绪的根**立刻**多根 `records` 进视口，不等终态——「实时」（plan 2026-09-10 §12）。生成的编排全在服务端，前端一根也不催。旧构建没有这条路由（404）按 api 对象记一次、以后直接走逐 SITE 老路；该库以 rocksdb 为准（409）只退这一次。任务与投影都只活在服务端进程内，重启后要重新点显示。它不是 `dbnums/{dbnum}/model/rebuild`——那是摄入形态、durable 队列的强制整库重建，前端不用。
_Avoid_: rebuild、整库重建、`/tasks` 列表轮询、订阅任务、把逐 SITE ensure 当成整库的契约

## 三维校审批注

**批注锚点**:
批注在模型空间中的定位参考中心，即图钉与引线的起点。它只回答“批注钉在哪里”，本身不构成批注关联。
_Avoid_: 关联元素、anchorRefno（裸字段名）

**问题目标元素**:
批注所指认的存在问题的模型元素，即角色为 member 的绑定对象。批注关联、反查与统计一律以问题目标元素为准。
_Avoid_: objectIds、锚点

**批注关联**:
批注与其问题目标元素之间的带角色绑定关系。同一构件可以同时充当批注锚点与问题目标元素，此时存在两条角色不同的绑定记录；text 批注点击命中的构件默认同时取得两种角色。
_Avoid_: refnos 列表、单向引用

**失效批注关联**:
问题目标元素或批注锚点的 refno 在当前模型版本中无法解析的绑定。它以降级样式呈现且仅允许重新绑定或移除；批注本身仍按坐标快照渲染。
_Avoid_: 静默落空、自动删除

**代表截图**:
挂在单条批注上的模型视角证据截图，回答“问题在哪、圈了什么、属于哪条批注”。批注标题与描述等文字不属于代表截图的内容。
_Avoid_: 所见即所得截图、任务附件
