# RUS-238 推送后开发方案任务计划

> 使用 planning-with-files 管理：本文件记录阶段计划；`findings.md` 记录事实发现；`progress.md` 记录执行日志。
> 可视化流程图：`rus-238-post-push-flow.html`。

## 目标

在已推送提交 `0819772 feat(measurement): 展示测量完整路径` 的基础上，完成真实数据与 PMS/编校审验收，并决定是否需要追加展示微调或性能保护。

## 当前状态

- 测量列表、确认回放、批注测量证据均已接入完整路径异步展示。
- 三处入口均保留 refno fallback。
- 中文 changelog 已更新并推送。
- 推送后的下一步计划文件已创建：`../2026-04-30-rus-238-post-push-next-plan.md`。
- 真实验收缺少外部输入：目标 BRAN、PMS 包名/任务单、角色、入口。
- 验收输入清单已创建：`acceptance-inputs.md`。

## Phase 1 · 验收输入收集

状态：阻塞。
执行进度：已创建可填写清单，等待输入。

任务：

- 获取目标 BRAN。
- 获取 PMS 包名或任务单。
- 明确验收角色，至少覆盖 SJ 和 JH。
- 明确验收入口：本地、仿 PMS 或真实 PMS。
- 准备距离、角度、批注证据、确认回放四类样例。

通过条件：

- 输入写入 `progress.md`。
- 可以用同一业务对象复核三处展示入口。

## Phase 2 · 本地真实模型验收

状态：待执行。

任务：

- 加载目标模型。
- 创建距离和角度测量。
- 检查测量列表完整路径、fallback 和 `title`。
- 验证定位、隐藏、恢复、删除行为。

通过条件：

- 无 `o:...:0` 暴露。
- lookup 成功时展示完整路径。
- lookup 失败时显示规范化 refno。
- 操作行为无退化。

## Phase 3 · 批注证据与确认回放验收

状态：待执行。

任务：

- 给批注关联距离和角度测量。
- 检查 `AnnotationWorkspace` 关联测量证据。
- 生成确认记录并打开 `TaskReviewDetail`。
- 对比测量列表、批注证据、确认回放的同一测量点文案。

通过条件：

- 三处入口展示规则一致。
- 批注定位按钮仍按原 `engine/id` 工作。
- 历史记录缺上下文时 fallback 正常。

## Phase 4 · PMS/编校审验收

状态：待执行。

任务：

- 从 PMS 或仿 PMS 入口进入三维校审。
- 使用目标 BRAN 生成或打开测量相关记录。
- 发起编校审并生成确认记录。
- 以校核入口重新打开，记录页面展示。

通过条件：

- PMS/编校审流程不暴露内部 object id。
- 页面截图或录屏可复核。
- 记录 BRAN、包名、角色、入口和实际展示文本。

## Phase 5 · 二次开发判断

状态：待执行。

根据验收结果决策：

- 路径过长：改为 tooltip 或次级文案。
- 请求过多：给 lookup 增加并发限制或批量查询。
- 路径无法 resolved：确认 fallback 是否足够交付。
- PMS 页面有差异：记录外部入口限制并补最小修复。

## Phase 6 · 工作区收敛

状态：只读盘点已完成。
执行结果：已完成工作区计数和主题分组，详见 `workspace-triage.md`。

任务：

- 单独盘点当前大量 RUS-238 之外的未提交/未跟踪变更。
- 按主题拆分为独立提交或独立任务。
- 不把无关变更混入 RUS-238 验收补丁。

继续条件：

- 若要提交 RUS-238 post-push 规划文件，需要明确提交授权。
- 若要清理或拆分整个工作区，需要另起独立收敛任务并逐组确认。

## 交付物

- `progress.md` 中的验收记录。
- PMS/编校审截图或录屏路径。
- 若有二次开发，补充最小代码改动与验证记录。
- 若无需二次开发，给出可交付结论。
