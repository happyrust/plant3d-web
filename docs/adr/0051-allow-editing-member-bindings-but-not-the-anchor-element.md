# 批注关联创建后可编辑而锚点构件不可变

批注创建后允许在详情中增删问题目标元素（复用点选/框选拾取），Q2 失效关联的「重新绑定」也走同一能力；批注锚点构件不可更换——更换锚点等于批注搬家，且几何签名变化会改变 annotationKey、破坏跨快照评论归并。

备选「创建时锁定不可编辑（现状）」被否决：绑错或漏绑只能删除重建，连带丢失评论、截图与审核状态；备选「锚点也可改」被否决：归并键风险高于收益。
