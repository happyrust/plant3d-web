# 尺寸系统按大型工厂模型规模设计

首版以单文档约 10,000 条已加载尺寸、单视口最多 2,000 条可见尺寸为设计预算；在 1920×1080、DPR 2 的参考桌面环境中，相机交互目标为 60 FPS，合并后的 layout → collision → paint 帧 p95 不超过 16 ms，pointer hit-test p95 不超过 2 ms。该目标取代 ADR 0018 的 500 条预算，要求文档事件增量化、失效驱动调度、视口裁剪、布局缓存、屏幕空间命中索引和有界碰撞，不允许交互帧无条件遍历或重建全部 10,000 条记录。
