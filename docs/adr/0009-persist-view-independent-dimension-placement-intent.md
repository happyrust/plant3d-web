# 只持久化视图无关的尺寸放置意图

`DimensionDocument` 只保存尺寸平面或投影方向、偏移距离、文字沿线参数及内外侧等视图无关的放置意图；跨标注碰撞避让、相机相关翻面和像素微调由各 `DimensionViewport` 独立维护。这样同一尺寸文档可在多个相机与视口中稳定呈现，代价是自动避让结果不会作为领域数据永久复现，显式 MBD 版式则继续通过 `LayoutResult` 逃生口表达。
