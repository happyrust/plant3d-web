# 尺寸锚点采用快照加可重解析语义引用

新尺寸记录的锚点同时保存设计坐标快照与可选的语义锚点（构件 refno、点源类型、候选点标识，沿用测量拾取源词汇）。渲染只读设计坐标快照，永不依赖在线解析；模型版本切换时对语义锚点做一次批量重解析，解析成功则更新快照使尺寸跟随构件，解析失败则把尺寸置入失效态，以降级样式提示用户，不静默钉在旧位置也不静默删除。

不采用 SolveSpace 式实时全关联，因为几何来自后端版本化数据，逐帧解析既昂贵也无必要；也不延续 V5 纯快照，因为版本切换后尺寸错位且用户无从察觉。代价是需要维护重解析流程与失效态的视觉语义。
