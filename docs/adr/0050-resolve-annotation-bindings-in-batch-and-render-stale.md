# 批注关联批量重解析并以失效态降级呈现

模型加载或版本切换后，对批注关联的 refno 做一次批量重解析；不可解析的绑定进入失效态，在批注面板与视口以降级样式提示（沿用尺寸系统 STALE 视觉语义），失效关联仅允许重新绑定或移除——不静默落空、不自动删除。批注渲染始终只读坐标快照，不依赖在线解析，与 ADR-0005/0029 的尺寸先例同构。

备选「不引入失效语义（现状）」与「仅导航时即时校验」被否决：两者下关联列表、统计与反查长期不可信，校审记录的可信度受损。
