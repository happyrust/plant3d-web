# 新尺寸系统完整验收后一次启用

**2026-07-22 实施状态：** `DIMENSION_V2_CUTOVER` 已恢复为默认关闭，开发验收继续通过 `dimension_demo=1` 进行。下一个生产候选是 rs-mbd 显式几何线性尺寸纵切；其余 Phase 2 图元和 Gate 5 检查在形成各自验收证据前不进入 cutover。

重建期间新系统只在开发开关或专用 demo 中接入，生产 UI 不显示部分完成的尺寸命令；线性、投影线性、角度、径向尺寸，以及 V5 迁移、校审后端往返、可访问列表、性能预算和 PNG/SVG 导出全部达到验收门槛后再一次启用。该选择接受较长的功能缺席期，以避免用户面对长期混合 schema、半套交互和逐类型迁移。
