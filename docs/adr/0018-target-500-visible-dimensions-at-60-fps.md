# 首版目标为 500 条可见尺寸下 60 FPS

> 状态：已由 ADR 0040 的大型工厂模型性能预算取代。

首版以单视口 500 条可见尺寸为规模预算：在约定的 1920×1080、DPR 2 参考桌面环境中，相机交互保持 60 FPS，一次完整 layout + collision + paint 的 p95 不超过 8 ms，pointer hit-test 的 p95 不超过 2 ms。该约束要求使用可基准验证的屏幕空间命中索引和有界碰撞算法，但不为 2000 条以上工程图规模引入渐进式布局或全局优化。
