# 从几何派生尺寸值并统一格式化标签

用户尺寸不保存可任意背离几何的 `textOverride`：尺寸值由类型和 Design Space 锚点几何派生，统一 `DimensionFormatPolicy` 负责显示单位、精度、`R`/`⌀`、`REF`、结构化前后缀及未来公差。外部只读来源可显式声明 authoritative text，但必须与普通派生标签在类型上区分。该选择消除单位与精度双真源，也意味着自定义说明应使用独立文字批注，而不是伪装成尺寸值。
