# 尺寸文字使用确定性的 LFF 矢量字形

新呈现内核从 SolveSpace `unicode.lff` 资产重新实现无状态的 glyph 解析与缓存，布局和 Canvas2D 画家消费同一份字形路径与精确 bounds；首版标签字符集覆盖尺寸数字、单位、`R`、`REF`、`°`、`⌀`及结构化前后缀。不得使用平台 `measureText()` 决定尺寸线留白，也不采用 DOM 文字叠层，从而保证文字绘制、命中和 golden 快照在不同浏览器与操作系统间一致。
