# 通过单一主题映射尺寸语义角色

尺寸记录只携带普通、外部参考、失效等语义角色，`DimensionTheme` 统一把角色及视口的 normal、hovered、selected 状态映射为颜色、线型、箭头、字体和像素常数；来源适配器不得传入任意原始绘制样式。单位、精度和符号继续由 `DimensionFormatPolicy` 管理，不进入主题。该选择限制逐尺寸自由美化，以换取唯一视觉真源和可完整接线的设置界面。
