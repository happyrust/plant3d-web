---
status: accepted
---

# 测量拾取层对齐 E3D Positioning Control：拾取过滤器 × 拾取类型两维模型，Any 只放行 Element / Ppoint / Pline，Graphics 细节从已加载网格派生，Intersect 按 E3D 分型做两（三）次子拾取求交

测量取点（Measure Distance / Angle / Perpendicular to 共用）的「哪些东西能被拾中、拾中后落在哪里」不再是一排点源开关，而是 E3D 3.1 Positioning Control 的**两维拾取层**（2026-09-13，方案 `docs/plans/2026-09-12-e3d-measure-parity-plan.md` Phase A）：**拾取过滤器**（`EDGPOSCNTRL.loadPicks` `picks[1..8]`：Any · Element · Aid · Pline · Ppoint · Screen · Graphics · External，顺序、标签与 E3D 面板一字不差）× **拾取类型**（`pickTypes[1..7]`：Snap · Distance · Mid-Point · Fraction · Proportion · Intersect · Cursor），外加 **Significant Snaps**（`intermediate`）一档；缺省 Any × Snap × Significant Snaps 开（Measure 的 `stdPosition` 拾取就是 `Any`，`pickTypeIndex = 1`）。Web 已有的点源按 E3D 的特征类归位而不是并列：`ptset` → Ppoint、`position` / `primitive_key_point` → Element、PLINE 端点配成线 → Pline、`mesh_pick_point` → Screen（Web 表面点）、新增 `mesh_graphics` → Graphics、新增 `tubing_axis`（直管管身轴线，E3D TUBING，特征类 `tubing`）→ 随 Element 放行；拾中的元素本身在要线的场合（Intersect 操作数 / Perpendicular 目标）按 E3D `line()` 给 P1 → P2（`elementLine`，见（5））；过滤器决定候选**准入**（`measurementPickFilterAdmits`），拾取类型决定拾中几何上**落点怎么派生**（`pickDerivation.ts`：点候选原样、线候选按近端 / 中点 / 等分 / 比例 / 走距离、面候选一律 射线 ∩ 平面），两维正交、互不知晓。提示条改成 `EDGSTATE.prompt()` 的结构 `<命令> · <步> (<拾取类型 token>) [Snap] : <目标>`（`距离测量 · 第 2/2 步 选择终点 (Mid-Point) Snap : ELBO P-Point #1`），过滤器**不进提示**、与 E3D 一致；拾取类型的取值（Distance mm / Fraction n / Proportion 0–1）与整个拾取层进测量样式仓 V9 持久化，未知或不可用的 id 回缺省。Aid / External 两档留在列表里灰掉并写明原因（Web 没有设计辅助系统，方案 §7 Q3 未拍板；外部几何不在本期），不从列表里删——用户看到的是 E3D 那张表。

五条具体口径是这条 ADR 要钉住的，因为每一条都有一个「更宽松、看起来更好用」的替代，而我们有意没选：

**（1）Any = Element / Ppoint / Pline（+ Web 表面点），不是「全部」。** `EDGPICK.stdAny` 的注释是「Standard pick interpreter for Element, Ppoint or Pline」（`inMode = 'pany'`），Graphics 细节要 `stdGraphics`（`inMode = 'pickdetail'`）、辅助要 `stdAid`、外部几何要 `stdExternal`——E3D 里 Any 从来不拾 facet 边 / 面。Web 照此：Any 放行 `element` / `ppoint` / `pline` / `surface`，**不放行** `graphics-line` / `graphics-plane` / `aid` / `external`；Graphics 边 / 面只在 Graphics 过滤器下参与，反过来 Graphics 过滤器下 P-Point / Item 原点一次都不出现（实机扫描 132 像素为证，golden MD §12）。表面点算进 Any 是 Web 的一处有意偏离：它是 E3D「附近没有显著点时拾元素本身」（自由表面态）与 `Element` × `Cursor`（`edgTypes.attribute(noun).exact()`，元素上的精确点）两种情形在 Web 的替身；`Element` 过滤器也只在 Cursor 类型下放行它，其它类型下 Element 就是**元素原点**——E3D 3.1 `EDGPICKTYPE` 的 ELEMENT 分支是 `edgTypes.attribute(fullType).snap()` 加 `handle any → item.position`（`edgpicktype.pmlobj` 338；exact / distance / proportion / fraction 489 / 1000 / 1145 / 1294 同款回落），而 `edgtypes.pmlobj` 里实现了 `snap()` 的 handler 只有 SCTN / GENSEC / PANEL / 挤出体 / 实体路径 / 船体 / REFGRID / Aid 几类，CYLI / DISH / SNOUT / NOZZ 只有 `arc` / `line`、ELBO / BEND / RTOR / CTOR 只有 `arc`、BOX / VALV / FLAN 无 handler，一律回落元素原点；Web 的 `position`（Item 原点）就是它。「盒角 / 面中心 / 圆柱轴端」这类基本体显著点是 legacy parquet `primitive_key_point` 带来的 Web 增强，不是 E3D 口径（2026-09-13 核对，决策 `d-336`；本条 2026-09-13 14:08 初稿写的「Element 就是显著点」据此改正）。被否决的替代：Any = 所有点源并集——一旦 Graphics 边进了缺省过滤器，12 px 内的网格棱会在整个模型上抢走 P-Point 的吸附（渲染细分的边处处都是），缺省体验反而变差，而且 E3D 也不是这么做的。

**（2）Graphics 过滤器从已加载网格派生，前端本地，不问后端。** E3D `pickdetail` 拾的是构件绘制细节：facet 边 → `3D_LINE`（`getLine()`）、facet → `PLANE`（`getPlane()`）。Web 没有 PDMS 图形核可问，gen-model 吐的是烘好的实体、没有逐 facet 细节，于是从 DTX 已经为渲染加载的三角网格派生（`src/measurement/graphics/meshFeatureGraphics.ts`，纯函数、无 three.js 依赖）：**绘制边** = 边界边（只有一个邻接三角形）/ 非流形边（≥ 3 个）/ 二面角 ≥ 30° 的边，度 2 顶点处的共线段合并成一条——盒棱是一条线、圆柱端面圆周仍是一圈短边、圆柱母线在粗细分（8 段，二面角 45°）下是线、细细分（36 段，10°）下消失，这正是 E3D 绘制边的粒度；**面** = 命中三角形所在平面，并 BFS 出边连通的共面片给高亮轮廓（盒面一整片，圆柱侧面一格）。命中规则：光标射线 12 px 内有绘制边 → 边候选（控制点 = 边上离射线最近处，带 `segment` + `direction` 供拾取类型内核与 Perpendicular 轴线用），否则一个面候选（控制点 = 命中点，带 `plane` + `outline`）；同屏幕位置的边只留离相机最近的一条（正视盒体时背面棱不抢前面棱）。派生结果按 objectId + 世界矩阵缓存。这样拾出的点标 `近似`（`model-surface` / approximate）：网格是渲染细分不是设计曲面，圆柱面上的「面」是一格平面。Perpendicular to 顺带得到 `facet-plane` provider（E3D `getPlane()` 分支，文案「所在平面」）。被否决的替代：服务端新开 `pickdetail` 端点（要 e3d-model 逐基本体吐 facet 拓扑，是 Phase A 最大的后端工作量且与 `element/keypoints` 抢同一份放置矩阵改造，见 §7 Q5——后者 2026-09-13 也定为不做，`d-336`；而绘制边从网格就能确定地算出来）；直接把渲染三角形的每条边当线（细分边处处可吸，无法区分棱与曲面）；屏幕空间做二维边缘检测（随相机变、无三维几何可给 Perpendicular / Intersect 用）。

**（3）Intersect 按 E3D 分型做两（三）次子拾取，交点是一个 POSITION。** 逐条对照 `EDGPICKTYPE.intersect`（`edgpicktype.pmlobj` 616–960）与 `EDGSTATE.minor`：每次子拾取先转操作数——带段的（Graphics 边 / PLINE / `3D_LINE`）→ LINE，带面的（facet / Aid 平面）→ PLANE，带方向的点（P-Point / 基本体轴，E3D 的 PPOINT / DPOINT / PIN）→ POINTVECTOR 线，**裸点 → 拒收且不消耗这一击**（E3D「Unable to convert item into a line or plane for intersection」）；首项入栈，提示 token 推到 `Intersection[2]`；两项即求交，**两面要第三项**（`golabel /nextPick`，`Intersection[3]`）；线 × 线 / 线 × 面平行 → E3D `(2,870)`，**只丢失败的那一击、首项保留**；三面无唯一交点 → `(2,874)`，**整个会话清空**（`numberOfPicks = 1`）；异面直线不算错，取第一条线上离第二条最近的点（`LINE.intersection` 的口径）。交点作为这一击的测量点进正常流程时 label「交点」、几何字段清空——E3D 回的是 POSITION，所以 Mid-Point / Fraction 这些线上派生不会再链到交点上；Esc 分层多出第一档「先放弃进行中的子拾取」；换拾取类型 / 过滤器、重新激活都重置会话；已有子拾取时悬停用当前项试算交点做预览（「交点（预览）」）。ARC 操作数（E3D 的弧 × 线）Web 暂无，拒收不消耗。状态机是纯函数 `src/measurement/kernel/intersectPickSession.ts`，12 条单测覆盖分型、序号、两种错误码与清空规则。被否决的替代：「任意两次拾取取最近点对」（不是 E3D 的语义，面 × 面会回一条线而不是点）；一律两击（E3D 两面需第三项，少一击就没有唯一解）；任何失败都清空（E3D 只在三面失败时清空，平行时保留首项让用户换一条线即可）；把交点当带几何的线候选继续派生（E3D 回 POSITION）。

**（4）TUBING 管身轴线只在 Any / Element 下参与，Pline 不放行；轴线由直管放置矩阵前端派生、两端吸邻接 P-Point。**（2026-09-13 切片 4 补入；用户 22:05 拍板「照 E3D 维持 Any / Element 放行、Pline 不放行」，决策 `d-318`，22:49 由 `d-331` 取代——口径不变，ATTA 合并落地、已知偏离改写。）E3D `EDGTUBING`（`edgtubing.pmlobj`）把隐含管当**一条线**拾：`line(dbRef, 'LEAVE')` = 构件 `lPosition` → 下一个非 ATTA 成员的 `aPosition`（注释明说用邻接构件的到达位置而不是隐含管长）；`snap` = 拾取线 ∩ 管线后取近端，`exact` = 交点本身，`distance / proportion / fraction` 走 `GMFLINE`，`intersect` 把它当 LINE 操作数；`lineExtended` 就是 `line`，Significant Snaps 对它无效。TUBING 是图形视图在**元素类拾取模式**（`stdAny` `inMode = 'pany'` / `stdElement` `'pick'`）下对隐含管回的拾取类型（`EDGPICKDATA.type = inMode.data[1]`，`data[2]` 给 arrive / leave），`EDGPICKTYPE` 的 snap / exact / intersect / distance / proportion / fraction 各按 `pickType eq 'TUBING'` 分派；`stdPline`（`inMode = 'pline'`）回的是 PLINE，不回隐含管。Web 照此：`tubing_axis` 点源归特征类 `tubing`，`measurementPickFilterAdmits` 只在 Any / Element 下放行，Pline / Ppoint / Graphics 下一次都不出现（实机对偶：Pline 过滤器下悬停直管 → `等待取点`，Graphics → `TUBI 面`，golden MD §14）；派生路径与 PLINE 线候选同一条（有两端点的拾中几何）：Snap 近端、Cursor 轴线上离光标最近处、Mid-Point / Fraction / Proportion / Distance 沿轴线、Intersect 转 LINE，Perpendicular to 得轴向 provider。几何来源：Web 没有分支拓扑，但有画出来的直管——`src/measurement/tubing/tubingAxis.ts`（纯函数）用直管对象的局部包围盒 × 放置矩阵得轴线段 + 半径（gen-model 直管 = 局部单位圆柱，缩放 `(r, r, L)`），再把两端各吸到容差 max(2 mm, 5 % 半径) 内最近的邻接 P-Point（一点只服务一端，吸成零长即放弃），对应 E3D leave → arrive 的口径；loader 按 noun `TUBI` / gen-model `is_tubi` 登记直管；候选按孔径边缘入围（E3D 在管身任何位置都回 TUBING），孔径内的 P-Point 仍赢。用户 14:27 的措辞「进 Pline 类候选」按「线类候选」实现而不是 Pline 过滤器，是有意偏离，已确认。**ATTA 按 E3D 跳过**（2026-09-13 22:49，`edfb0b0`）：`EDGTUBING.line` 的远端是下一个**非 ATTA** 成员的 arrive，所以 ATTA 是穿过点——拾中段某端校正到 ATTA 的 P-Point 时，把同一构件下与之共线（0.5° 内）、端点相接的另一段（们）接成一条线（`mergeTubingAxisAcrossPassThrough`），OLET 这类非 ATTA 的零长构件不穿过；认出 ATTA 靠点集响应透传的构件 noun（ATTA 没有几何、不在 DTX 登记里）。实机核对：gen-model 画直管时本就跳过非 SPKBRK 的 ATTA / STIF / BRCO（`cata_model.rs`），BRAN 24381_145018 的 6 个 ATTA 全在直管轴线内部、本库 169 个 ATTA 无 SPKBRK，合并只在预制分段（SPKBRK）处起作用（golden MD §15）。残余偏离（记在案、不改口径）：gen-model 还跳过非 SPKBRK 的 STIF / BRCO，而 E3D 3.1 `line()` 只跳 ATTA——在这两类构件处 Web 轴线比 E3D 管线长（本库无样本，未处理）；BRAN HEAD 管与 LEAVE 管同一处理。被否决的替代：Pline 过滤器也放行（E3D `stdPline` 不拾隐含管，且会让 gen-model-v1 下本来为空的 Pline 过滤器冒出非 PLINE 的线）；服务端在 `element/ptset` 的 `members` 里附管身端点（前端从放置矩阵就能确定地算出来，服务端不需改）；只把两端点当点候选（丢掉 Cursor / Mid-Point 等沿线派生，不是 E3D「一条线」的语义）。

**（5）元素类拾取拾中的元素当 Intersect / Perpendicular 操作数时按 E3D `line()` 转 P1 → P2；只当操作数，不改 Snap；无点集的设计基本体从局部帧几何派生。**（2026-09-14 切片 5 补入，`79b199c`，决策 `d-373`；用户 2026-09-13 23:16「先做前端的 Element 拾中 CYLI / NOZZ / DISH / SNOUT 的 P1 → P2 line 操作数（Intersect / Perpendicular），带 vitest 与实机」，是 Q5 `d-336` 定下的前端后续。）E3D `EDGPICKTYPE.intersect` 的 ELEMENT 分支（`edgpicktype.pmlobj` 631）与 `edgpositiondata.line()` 调 `edgTypes.attribute(fullType).line(item)`，实现了 `line(DBREF)` 的 handler 只有 `EDGCYLINDER`（CYLI / NCYL / SLCY / NSLC）、`EDGDISH`、`EDGSNOUT`（CONE / SNOU）、`EDGNOZZLE`、`EDGPYRAMID`，**每一个都回 `pPosition[1] → pPosition[2]`**（`edgcylinder.pmlobj` 163 / `edgdish` 91 / `edgsnout` 177 / `edgnozzle` 164 / `edgpyramid` 57）；ELBO / BEND / RTOR / CTOR 只有 `arc()`，BOX / VALV / FLAN / TEE 没有 handler → `handle any` 拒收（「Unable to convert item into a line or plane for intersection」，与（3）裸点同一处置：不消耗这一击）。Web 照此：候选 / 命中多一格 `elementLine`（`src/measurement/kernel/elementLine.ts`：noun 表 + `elementLineFromPPoints`），Intersect 操作数 `segment` 优先、其次 `elementLine`，Perpendicular to 的目标线 = 元素 P1 → P2（过 P1、不过拾中点，`targetLabel`「CYLI 轴线（P1 → P2）」）；元素类过滤器（Any / Element）下光标落在元素任意处就是 E3D 的 ELEMENT 拾取，所以没有候选胜出、甚至表面点源关着时，要线的那一击（Intersect 类型或 Perpendicular 正等第二点）仍把命中的元素转线，Element × Intersect 下表面点因此当元素拾取放行；**Snap / Mid-Point 一概不变**——E3D 的 ELEMENT `snap()` 对这些元素回落元素原点（（1）），这条线不是线候选。几何来源：目录件（NOZZ）用点集的 P1 / P2；gen-model-v1 的 `element/ptset` 只解目录 PTSE，**设计基本体没有点**（实机 CYLI / CONE / DISH / PYRA 全部 0 点），而 gen-model 把它们画在基本体自己的局部帧（CYLI 单位圆柱实例 z ∈ [0, 1]；CONE / SNOU `gen_snout` 端环 z = ∓HEIG / 2、XOFF / YOFF 两端各摊一半；DISH 底面 z = 0 顶点 z = HEIG；PYRA 同 snout）× 放置矩阵，E3D 的 P1 / P2 又都在两端面中心，所以**截面对中于局部原点时局部 z 向包围盒范围就是 P1 → P2**——圆柱 / 锥 / 碟要对中且圆、棱锥只要对中（`elementLineAcceptsLocalBounds`，相对容差 1e-3），带 XOFF / YOFF 的 SNOU / PYRA 与烘在世界帧的实体不派生（被拒）。实机（golden MD §16）：10 CYLI / 4 CONE / DISH / PYRA 的派生轴线与 `element/attributes` + `world_transform` 解析的 P1 / P2 Δ ≤ 0.001 mm；CYLI × CYLI、CYLI × CONE 交点与解析 Δ 0.001 mm，Perpendicular to CYLI / DISH 垂足到轴 ≤ 0.001 mm；DISH / PYRA 接受、BOX 拒收不消耗。已知偏离：带偏移的 SNOU / PYRA 在 E3D 里是斜的 P1 → P2，Web 认不出偏移就不给线（要 `element/attributes` 的 XOFF / YOFF 或服务端给设计基本体合成 P1 / P2）；无目录的 NOZZ gen-model 不出几何、也无点；被负体切掉端面的元素包围盒 z 范围会短于 HEIG。被否决的替代：把这条线也当 Snap / Mid-Point 的线候选（E3D Element × Snap 是元素原点，会让 Element 过滤器下的 Snap 从原点变成轴端）；只在有点集时给线（gen-model-v1 下设计基本体一律没有，功能等于不存在）；用网格顶点拟合轴线（渲染细分不是设计曲面，包围盒对局部帧几何已是精确的）；为拿 XOFF / YOFF 悬停时再请求 `element/attributes`（首次悬停认不出、多一次往返，且本库无样本）。

**证据等级**：以上规则全部是 `static_expectation`——出自 E3D 3.1 PMLLIB `edgposcntrl.pmlobj` / `edgpick.pmlobj` / `edgpicktype.pmlobj` / `edgstate.pmlobj` / `edgtubing.pmlobj` / `edgpickdata.pmlobj` / `edgtypes.pmlobj` 与各 `edg*.pmlobj` handler 的 `line()` 的源码阅读，**不是**运行时观测；G7-02（EDGE / PLANE / TUBING 实际捕捉几何）、G8（拾取类型位置字串、Intersect 交点，含元素操作数）、G9（Element × Snap 回元素原点；原「Element 显著点」按 `d-336` 改题）三组 E3D 运行时 golden 都还未采（E3D 进程不在跑）。任何一条与 golden 对不上时改 Web、改这条 ADR 的措辞，不改 golden。

不做的：不建 Aid 系统、不做 External（列表里灰掉，§7 Q3 另议）；不做服务端 `pickdetail`；**不做 `element/keypoints`**（E3D Element × Snap 是元素原点，基本体显著点不是 E3D 口径，要这体验另立 Web 增强、走前端从 DTX 放置矩阵派生而不改 e3d-model；用户 2026-09-13 拍板，决策 `d-336`）；`element/plines`（SCTN / GENSEC 的截面 PLINE 线，`edgsctn.snap` 沿它取点）排在后面，不在本条范围（Element 拾中元素的 P1 → P2 `line()` 操作数 2026-09-14 已做，见（5））；带 XOFF / YOFF 的 SNOU / PYRA 的斜 P1 → P2 不做（见（5）已知偏离）；TUBING 在 STIF / BRCO 处按 E3D 截断不做（见（4）残余偏离），DPOINT 不在本条范围；Positioning Control 的 Offset / Working plane / grids 是定位能力，不进测量。

依据：方案 §1.5「拾取层（Positioning Control，测量取点共用）」与 Phase A（用户 2026-09-12 批准的对齐方案）；用户 2026-09-13 12:43「写 ADR，把 Any=Element/Ppoint/Pline、Graphics 网格派生、Intersect 分型记成决策」；用户 2026-09-13 14:27「接着做 TUBING 轴线点前端派生」与 22:05「照 E3D 维持 Any / Element 放行、Pline 不放行」（决策 `d-318` → `d-331`）；用户 2026-09-13 Q5 拍板「不做 `element/keypoints`，排 `element/plines` + Element 拾取的 P1 → P2 line 操作数」（决策 `d-336`，本条（1）「Element 就是显著点」据此改为元素原点）；用户 2026-09-13 23:16「先做前端的 Element 拾中 CYLI / NOZZ / DISH / SNOUT 的 P1 → P2 line 操作数（Intersect / Perpendicular），带 vitest 与实机」（本条（5））；golden MD `docs/verification/e3d-measurement-runtime-golden-capture.md` §12（Graphics 实机：`面 → 边` 2311 mm 独立复算一致）、§13（Intersect 实机：`ELBO 边 × VALV 边` 出交点）、§14（TUBING 实机：`轴线 → 轴线` Snap 4568 mm / Mid-Point 3242 mm 独立复算一致，校正后的轴线端点与 §11 E3D ELBO 145031 P1 差 0.001 mm）、§15（TUBING × ATTA：跨 ATTA 直管 Snap / Mid-Point 与 E3D 线端点 / 中点差 0.000 mm，页内内核用实机坐标切开再合 Δ 0.0000 mm）、§16（元素 `line()` 操作数：CYLI × CYLI / CYLI × CONE 交点、Perpendicular to CYLI / DISH 垂足与解析 P1 / P2 Δ ≤ 0.001 mm）。落地：`src/measurement/pick/pickLayerModel.ts`（两维模型、可用性、提示结构、`measurementPickFilterAdmits`）、`src/measurement/kernel/pickDerivation.ts`（拾取类型派生）、`src/measurement/graphics/meshFeatureGraphics.ts`（绘制边 / 面派生）、`src/measurement/tubing/tubingAxis.ts`（管身轴线派生与端点校正）、`src/measurement/kernel/elementLine.ts`（元素 `line()` 的 noun 表、P1 → P2、局部包围盒规则）、`src/measurement/kernel/intersectPickSession.ts`（Intersect 状态机）、`src/measurement/kernel/perpendicularTargetProvider.ts`（`facet-plane`）、`src/composables/useMeasurementPickSources.ts`（点源 → 特征类映射、`mesh_graphics` / `tubing_axis` 候选）、`src/composables/useDbnoInstancesDtxLoader.ts`（直管登记）、`src/composables/useXeokitMeasurementTools.ts`（接线、子拾取、高亮、Esc 分层）、`src/composables/useXeokitMeasurementStyleStore.ts`（V9）、`src/components/tools/MeasurementOverlayBar.vue`（过滤器 × 类型控件）。提交 `9d34701` → `51b6933` → `001b302` → `6ca770a` → `e91b30e` → `743d922`（本 ADR）→ `b83d821`（TUBING 代码）→ `2f0bb31`（TUBING docs 与走查产物）→ `dd8849f`（本条（4））→ `edfb0b0`（TUBING 跨 ATTA 合并 + noun 透传）→ `8674f04`（§15 docs）→ `6ae3861`（Q5 措辞）→ `79b199c`（元素 `line()` 操作数，本条（5））。
