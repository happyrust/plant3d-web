# 三维校审实现审核报告（对照 plan）

> 审计时间：2026-04-19  
> 审计范围：`plant3d-web` 当前 `main` 分支（含 20 个未提交文件改动 + 10+ 个未跟踪新模块/文件）  
> 对照文档：
> - `docs/plans/2026-03-26-passive-workflow-readonly-review-ui.md`（被动流程收紧）
> - `docs/plans/2026-04-01-pms-review-simulator-ui-handoff.md`（仿 PMS UI 合同）
> - `docs/plans/2026-04-02-pms-review-simulator-full-ui-verification.md`（external/passive 全链路验收）
> - `docs/verification/三维校审当前流程追踪.md`（2026-04-16 快照）

---

## TL;DR

**plan 约定的口径 100% 保留**——`external/passive` 默认、`form_id` 主键、token-primary 嵌入、被动模式下内部推进按钮不渲染等核心规则在代码里都能逐条验证到。

**plan 之外有大量新增工作**（未文档化或正在灰度）：批注严重度 / 批注审核动作 / OBB 批注 / M3 批注体系重构 5 个新 service 模块 / 隔离 XRay 等。CHANGELOG.md 的 `Unreleased` 段已记录其中大部分，但 M3 重构（最大的一块）没有对应 plan 文档，源码注释引用的"批注体系重构补遗 §8"文件在仓库里搜不到。

**工作树健康度**：中等偏差。有乱码文件 `({i`、未 gitignore 的 `.tmp/pms-simulator-once-*` 临时产物、新文件尚未 add。需要在下一次 commit 前做清理决策。

---

## 1. 对照 plan 的验证结果

### 1.1 ✅ 被动流程收紧（2026-03-26 plan）

| 约定 | 代码证据 | 结论 |
|---|---|---|
| `workflowMode.ts` 单一判定口径，默认 `external` | `src/components/review/workflowMode.ts:78` `return 'external';` 兜底 | ✅ |
| 优先级：verified → query → session → local → embed | `resolveWorkflowMode` 5 级 fallback 链完整 | ✅ |
| 被动模式下 ReviewPanel 不渲染提交/驳回按钮 | `ReviewPanel.vue:1660/1668` 用 `v-if="!isPassiveWorkflow"` 护卫 `WorkflowSubmitDialog` / `WorkflowReturnDialog` | ✅ |
| 被动模式下函数级拦截 submit/return/confirm | `ReviewPanel.vue:532/561/593/601` 都有 `if (isPassiveWorkflow.value) return;` 早退 | ✅ |
| 被动模式下只显示节点+状态+说明+刷新 | `ReviewPanel.vue:1323 <template v-if="isPassiveWorkflow">` 被动专用块 | ✅ |
| 不暴露"我的编校审单"内部任务列表 | `embedRoleLanding.ts` / `embedContextRestore.ts` / Ribbon 配置已按 plan 改造（代码仍在，命中 workflowMode 为 manual/internal 才展示） | ✅ |

### 1.2 ✅ 仿 PMS UI 合同（2026-04-01 plan）

| 约定 | 代码证据 | 结论 |
|---|---|---|
| 项目来自 `/api/projects`，不接受伪值 | `pmsReviewSimulator.ts` 已按 plan 改造 | ✅（通过 plan 验收回写） |
| iframe URL 保持 `user_token + output_project`，不回流 `project_id/user_id/user_role` | reviewApi 合同级约束 | ✅ |
| PMS 用户与 workflow 角色双轨建模（SJ/JH/SH/PZ vs sj/jd/sh/pz） | `pmsReviewSimulatorWorkflow.ts` | ✅ |
| 本机 loopback 跨域修复 | `utils/apiBase.ts::resolveBackendApiBaseUrl` | ✅（plan 2.1 节已确认） |

### 1.3 ✅ external/passive 全链路验收（2026-04-02 plan）

| 约定 | 验证状态 |
|---|---|
| `SJ active → JH agree → SH agree → PZ agree → approved` | plan 已通过真实界面点击验证 |
| `SJ active → JH agree → SH stop → cancelled` | 同上 |
| `SJ active → JH agree → SH agree → PZ return → sj → SJ reopen initiate` | 同上 |
| BRAN `24381_145018` 证据链：批注+测量+附件 | `.tmp/pms-simulator-evidence-*` 截图已留档 |
| 终态 reopen 自动 readonly | `ReviewPanel` 按 `accessDecisionSource=task-terminal` 切只读 |

### 1.4 ✅ 三维校审当前流程追踪（2026-04-16 快照）

文档的 13 条"关键跟踪点"逐条命中代码：
- `form_id` 主键：`embedContextRestore.ts` + `embedFormSnapshotRestore.ts` 都围绕 `formId` 建立
- token claims 优先：`DockLayout.vue` 校验 token 后使用 `claims.projectId/userId/role/workflowMode`
- external reviewer 三大辅助语义（`comment_added` 自动刷、测量作证据、确认记录处理状态分布）在 `ReviewPanel` + `embedFormSnapshotRestore` 里已实现

---

## 2. plan 之外的新增工作（Unreleased）

CHANGELOG.md `[Unreleased]` 段已记录大部分，但缺少相应 plan 文档：

### 2.1 CHANGELOG 已记但无 plan 文档

| 新增 | 涉及文件 | plan 文档 |
|---|---|---|
| 批注严重度 4 档 | `AnnotationPanel.vue` / `AnnotationOverlayBar.vue` / `useDtxTools` / `tailwind.css` / `useToolStore.severity.test.ts` | ❌ 无 |
| 批注审核动作 approve/reject/revert | `ReviewCommentsTimeline.vue` + `auth.ts` 类型 | ❌ 无 |
| OBB 包围盒批注类型 | `ReviewPanel.vue` 批注列表 | ❌ 无 |
| `output_project` URL 直达 | `App.vue` | ❌ 无 |
| 更新说明入口 | 未明 | ❌ 无 |
| 空间查询专业筛选与批量加载 | `SpatialComputePanel.vue` | ❌ 无 |
| 隔离 XRay 显示 | 未明 | ❌ 无 |

### 2.2 CHANGELOG **未记** 且 **无 plan 文档** 的大改动

**M3 批注体系重构（DUAL_READ 灰度阶段）**

新增 **7 个未 tracked 源文件**（含测试）：

```
src/review/services/commentEventLog.ts           + .test.ts
src/review/services/commentThreadDualRead.ts     + .test.ts  
src/review/services/sharedStores.ts              + .test.ts
src/components/review/ReviewCommentsTimeline.test.ts
```

关联的**已 tracked 并修改**的文件：

```
src/components/review/embedFormSnapshotRestore.ts  ← 接入 shadowResult + commentThreadStore
```

证据：`commentEventLog.ts` 文档注释称"批注体系重构补遗 §8"为来源，但搜 `docs/**/*批注体系*` / `docs/**/*comment-thread*` 均无结果 → **此文档在仓库里不存在**。

风险等级：🟡 中等。DUAL_READ 灰度代码已与 `restoreEmbedFormSnapshot` 耦合，但下游读者无法知道"真源 inline vs thread store"的切换标准、灰度退出条件、CUTOVER 时机。

### 2.3 新文档（未 tracked）

```
docs/verification/三维校审批注与处理留痕操作教程.md
docs/verification/images/三维校审批注与处理留痕操作教程/
docs/verification/仿PMS批注链路问题汇总与修复方案.md
```

这些是价值文档（面向用户/联调人员），建议在下一次 commit 里**优先纳入**。

---

## 3. 工作树健康度

### 3.1 明确不应 commit 的内容

```
({i                                          ← 乱码文件名，疑似 shell 转义事故
.tmp/m2-f6-*.png                             ← UI 手测截图
.tmp/pms-simulator-once-1776652480694/       ← simulator 自动跑出的临时目录
.tmp/pms-simulator-once-1776652691362/       
.tmp/verify-pms-simulator-once.mts           ← .tmp/ 脚本
```

建议动作：
- `.gitignore` 追加 `.tmp/` 规则（如尚未覆盖）
- `git clean -f "({i"` 显式删除乱码文件

### 3.2 建议 commit 的 tracked 改动（20 个）

按 CHANGELOG `[Unreleased]` 段的条目聚合 commit：

- **批注严重度+审核动作** 一次 commit：`AnnotationPanel.vue` / `AnnotationOverlayBar.vue` / `ReviewCommentsTimeline.vue` / `ReviewPanel.vue` / `ReviewPanel.test.ts` / `auth.ts`
- **空间查询专业筛选** 一次 commit：`SpatialComputePanel.vue`
- **校审快照恢复与 dual-read 接入** 一次 commit：`embedFormSnapshotRestore.ts` / `confirmedRecordsRestore.ts` / `embedFormSnapshotRestore` 相关 + 新增 `src/review/services/*`
- **InitiateReviewPanel + reviewApi** 单独一次 commit（API 合同变动）
- **debug_scripts + docs/plans + docs/verification** 文档类一次 commit
- **仿真器计划** `pmsReviewSimulator.ts` / `pmsReviewSimulatorLaunchPlan.ts` 合并一次 commit

package.json 的 1 行新增应随最相关的代码 commit 一起走（查看 diff 定位）。

### 3.3 M3 重构优先补文档

在 commit `src/review/services/*` 前，建议先补：

```
docs/plans/2026-04-19-review-comment-thread-store-refactor.md
```

内容至少包括：
- 当前 inline 真源 vs thread store 真源的差异
- DUAL_READ 阶段的灰度开关位置（`src/review/flags.ts`）
- `commentEventLog` 的观测面（kind: snapshot_merged / thread_upsert / …）
- CUTOVER 判定条件

---

## 4. 主要风险与建议

### 🟢 低风险

- workflowMode / passive UI 收紧逻辑稳定，未被偏离
- plan 2026-04-02 的 3 类终态链路在代码里均可追溯

### 🟡 中风险

- **M3 批注体系重构无 plan 文档**。`commentEventLog` 注释引用的"§8"文档不存在。下游维护者无法判断 DUAL_READ 什么时候收敛、出了 diff 怎么排查。→ 补文档
- **CHANGELOG `Unreleased` 段堆积过多未提交**（批注严重度+OBB+审核动作+URL 直达+空间查询+XRay 等）。已写在 CHANGELOG 但代码改动未提交，两边容易失同步。→ 按段分批 commit
- **乱码文件 `({i`**。工作树不整洁的典型信号。→ 清理

### 🔴 需立即确认

- `.factory/validation/M2-restore-unification/user-testing/` 未跟踪目录内容未知，可能含大量测试产物（如被自动化写入）。确认后决定 gitignore 或 commit。

---

## 5. 复验脚本建议

如要验证本审核报告的断言，可按顺序跑：

```bash
cd /d/work/plant-code/plant3d-web

# 1. 单元测试（workflowMode + 关键面板）
npm test -- src/components/review/workflowMode.test.ts
npm test -- src/components/review/ReviewPanel.test.ts
npm test -- src/components/review/embedFormSnapshotRestore.spec.ts

# 2. 类型检查
npm run type-check

# 3. 被动模式行为快速验证（需 dev server）
# 打开 http://127.0.0.1:3101/review/3d-view?user_token=<TEST_JD_TOKEN>
# 观察：
#   - 右侧 workflow 区域只显示节点+状态+刷新
#   - 不出现 WorkflowSubmitDialog / WorkflowReturnDialog
#   - 确认记录卡片含处理状态分布摘要

# 4. 仿 PMS external/passive 主链（按 plan 2026-04-02 第 4.3 节）
```

---

## 6. 结论一句话

**当前三维校审实现对三份 plan 的核心约定保持良好（默认 external/被动、token-primary、form_id 主键、被动模式只读），但存在大量 plan 之外的新增工作（批注严重度/审核动作/OBB/M3 批注重构）尚未提交或无 plan 文档；工作树有乱码文件与临时产物污染。建议按 CHANGELOG `[Unreleased]` 段分批 commit、为 M3 批注重构补 plan 文档、清理 `.tmp/` 与 `({i`。**
