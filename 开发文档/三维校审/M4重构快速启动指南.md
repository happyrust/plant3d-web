# M4 重构快速启动指南

> 生成时间：2026-03-17  
> 目标：快速启动 M4 审核工作台重构

## ✅ 已完成

### 1. 创建了子组件目录
```
src/components/review/sections/
```

### 2. 创建了 4 个子组件

#### TaskContextSection.vue（任务上下文区）
- 展示任务标题、描述
- 展示模型名、发起人、校核人、审核人
- 展示当前节点、状态、优先级
- 展示创建/更新时间
- 展示关联构件数量

#### WorkflowActionSection.vue（流转动作区）
- 提交按钮（动态文案）
- 打回按钮
- 刷新按钮
- 集成提交/打回弹窗

#### ConfirmedRecordsSection.vue（确认记录区）
- 确认记录列表
- 批注数量统计
- 删除单条记录
- 清空全部记录

#### DataSyncSection.vue（数据同步区）
- 导出按钮
- 导入按钮
- 覆盖模式选项

## 🔄 下一步行动

### 1. 创建剩余子组件（1 天）

#### AuxiliaryDataSection.vue（辅助数据区）
需要创建，包含：
- 碰撞查询
- 辅助数据查询
- 结果列表
- 定位/高亮功能

### 2. 重构 ReviewPanel.vue（1 天）

将现有的 1155 行简化为约 200 行，集成所有子组件：

```vue
<script setup lang="ts">
import TaskContextSection from './sections/TaskContextSection.vue';
import WorkflowActionSection from './sections/WorkflowActionSection.vue';
import ConfirmedRecordsSection from './sections/ConfirmedRecordsSection.vue';
import DataSyncSection from './sections/DataSyncSection.vue';
// import AuxiliaryDataSection from './sections/AuxiliaryDataSection.vue';

// ... 简化的逻辑
</script>

<template>
  <div class="review-panel">
    <TaskContextSection :task="currentTask" />
    <WorkflowActionSection 
      :task="currentTask"
      @submit="handleSubmit"
      @return="handleReturn"
    />
    <ConfirmedRecordsSection
      :task-id="currentTask?.id"
      :records="confirmedRecords"
      @delete="handleDeleteRecord"
    />
    <DataSyncSection
      @export="handleExport"
      @import="handleImport"
    />
  </div>
</template>
```

### 3. 编写测试（1 天）

为每个子组件创建测试文件：
- `TaskContextSection.test.ts`
- `WorkflowActionSection.test.ts`
- `ConfirmedRecordsSection.test.ts`
- `DataSyncSection.test.ts`

### 4. 验证与优化（1 天）

- 手动测试所有功能
- 性能优化
- 代码审查

## 📝 使用示例

### TaskContextSection
```vue
<TaskContextSection 
  :task="currentTask"
  :loading="isLoading"
/>
```

### WorkflowActionSection
```vue
<WorkflowActionSection
  :task="currentTask"
  :loading="isSubmitting"
  @submit="handleSubmit"
  @return="handleReturn"
  @refresh="handleRefresh"
/>
```

### ConfirmedRecordsSection
```vue
<ConfirmedRecordsSection
  :task-id="currentTask?.id"
  :records="confirmedRecords"
  :loading="isLoading"
  @delete="handleDeleteRecord"
  @clear-all="handleClearAll"
  @refresh="handleRefresh"
/>
```

### DataSyncSection
```vue
<DataSyncSection
  :loading="isSyncing"
  @export="handleExport"
  @import="handleImport"
/>
```

## 🎯 预期效果

### 重构前
- `ReviewPanel.vue`: 1155 行

### 重构后
- `ReviewPanel.vue`: ~200 行（-83%）
- `TaskContextSection.vue`: ~150 行
- `WorkflowActionSection.vue`: ~120 行
- `ConfirmedRecordsSection.vue`: ~130 行
- `DataSyncSection.vue`: ~80 行
- `AuxiliaryDataSection.vue`: ~250 行（待创建）

**总计**: ~930 行（减少 225 行，结构更清晰）

## ⚠️ 注意事项

1. **保留备份**：重构前备份原文件
2. **增量测试**：每完成一个子组件就测试
3. **类型安全**：确保所有 Props/Emits 类型正确
4. **样式一致**：使用 Tailwind CSS 保持样式统一
5. **事件命名**：使用清晰的事件名称

## 🚀 立即开始

```bash
# 1. 创建重构分支
git checkout -b refactor/m4-review-panel-split

# 2. 备份原文件
cp src/components/review/ReviewPanel.vue src/components/review/ReviewPanel.vue.backup

# 3. 运行测试基线
npm test -- src/components/review/ReviewPanel.test.ts

# 4. 开始重构
# 已创建 4 个子组件，继续创建 AuxiliaryDataSection.vue
# 然后重构 ReviewPanel.vue 主组件
```

## 📚 参考文档

- `M4重构执行方案-2026-03-17.md` - 完整重构方案
- `三维校审M4审核工作台基础版任务单.md` - M4 任务单
- `下一步行动清单-2026-03-17.md` - 行动清单
