# 三维校审流程与批注处理审核

> 日期：2026-04-19  
> 适用仓库：`plant3d-web`（前端）+ `plant-model-gen`（后端 Rust）  
> 定位：对当前线上代码"三维校审主流程"和"批注体系"做现状梳理。仅描述事实，不重复 M2/M3/M4 重构计划。

## 1. 总览

```mermaid
flowchart LR
    subgraph 入口
        A1[平台内 reviewer 工作台<br/>?project_id&show_dbnum] -->|setCurrentTask| TS
        A2[PMS 嵌入<br/>?embed=1&form_id&token&workflow_mode] -->|workflow/sync| TS
        A3[离线导入包<br/>JSON payload] --> TS
    end

    subgraph 前端_useToolStore
        TS[draft 批注 / 评论 / 测量<br/>scope=project+db]
    end

    subgraph 前端_useReviewStore
        RS1[currentTask]
        RS2[confirmedRecords 任务级]
        RS3[reviewHistory]
        RS4[wsConnected / onCommentAdded]
    end

    subgraph 后端_REST
        BR1[(review_tasks)]
        BR2[(review_records)]
        BR3[(review_comments)]
        BR4[(review_workflow_history)]
        BR5[(review_form_model<br/>review_attachment)]
    end

    TS -->|确认 confirmCurrentData| RS2
    RS2 -->|POST /records| BR2
    TS -->|评论 CRUD| BR3
    RS1 -->|submit/return/approve/reject| BR1
    BR1 --> BR4
    A2 -.->|workflow/sync 聚合读| BR2 & BR3 & BR5
    BR3 -.->|comment_added WS| RS4
```

## 2. 工作流节点与状态机

### 2.1 节点链

线性链路（plant-model-gen `review_api.rs:2670 get_next_node`）：

```
sj (编制) → jd (校对) → sh (审核) → pz (批准) ─┘ end
```

退回（`return_to_node` + `can_return_to`）：当前节点可以退回到任意更早的节点。

### 2.2 任务状态机

```mermaid
stateDiagram-v2
    [*] --> draft: create_task<br/>(node=sj)
    draft --> submitted: submit_to_next<br/>(node sj→jd)
    submitted --> in_review: start_review<br/>(node=jd)
    in_review --> in_review: submit_to_next<br/>(node jd→sh→pz)
    in_review --> approved: submit_to_next 在 pz<br/>action=approve
    in_review --> rejected: reject_task
    in_review --> draft: return_to_node<br/>(回退到任意更早节点)
    draft --> cancelled: cancel_task
    submitted --> cancelled: cancel_task
    in_review --> cancelled: cancel_task
    approved --> [*]
    rejected --> [*]
    cancelled --> [*]
```

### 2.3 角色到节点的权限映射

来源：`submit_to_next_node` 的 `has_permission` 分支（`review_api.rs:2742`）

| 节点 | 允许操作的字段 |
|---|---|
| `sj` | `requester_id` |
| `jd` | `checker_id` 或 `reviewer_id` |
| `sh` | `approver_id` |
| `pz` | `approver_id` |

前端嵌入入口角色映射（`embedRoleLanding.ts:156`）：

| 后端 role | 前端 landing target |
|---|---|
| `sj` | `designer` |
| `jd / jh / sh / pz / admin` | `reviewer` |

## 3. 入口流程

### 3.1 平台内 reviewer 工作台

```mermaid
sequenceDiagram
    participant U as 用户
    participant TL as ReviewerTaskList
    participant RS as useReviewStore
    participant TS as useToolStore
    participant API as plant-model-gen
    participant V as Viewer

    U->>TL: 选择任务
    TL->>RS: setCurrentTask(task)
    RS->>API: GET /records/by-task/{taskId}<br/>+ /tasks/{id}/history
    API-->>RS: ConfirmedRecord[] + WorkflowStep[]
    RS->>RS: confirmedRecords / reviewHistory 更新
    RS->>WS: connectWebSocket(reviewerId)
    Note over RS,V: confirmedRecordsRestore 监听<br/>currentTaskId + records 变化
    RS-->>V: waitForViewerReady
    RS->>TS: importJSON(buildReplayPayload)
    TS->>V: syncFromStore()
```

### 3.2 PMS 嵌入恢复

```mermaid
sequenceDiagram
    participant URL as URL ?embed=1&form_id&token&workflow_mode
    participant LP as embedRoleLanding
    participant ER as restoreEmbedFormSnapshot
    participant API as plant-model-gen
    participant TS as useToolStore
    participant V as Viewer

    URL->>LP: readEmbedModeParamsFromSearch
    LP->>LP: resolveTrustedEmbedIdentity (verify token)
    LP->>LP: resolveEmbedLandingTargetFromRole(role)<br/>→ designer | reviewer
    LP->>ER: restoreEmbedFormSnapshot({ formId, token, actor })
    ER->>API: POST /api/review/workflow/sync<br/>(action=active|agree|return|stop)
    API-->>ER: { records, annotationComments,<br/>attachments, models, currentNode, taskStatus }
    ER->>TS: importTools(buildWorkflowSnapshotReplayPayload)
    TS->>V: syncFromStore()
    Note over ER,V: 评论通过 mergeWorkflowCommentsIntoRecords<br/>挂载到 record.annotation.comments
```

## 4. 批注实体模型

### 4.1 类图

```mermaid
classDiagram
    class AnnotationRecord {
        +id : string
        +entityId : string
        +worldPos : Vec3
        +visible : bool
        +glyph : string
        +title / description
        +reviewState? : AnnotationReviewState
        +severity? : AnnotationSeverity
        +comments? : AnnotationComment[]  «inline»
    }
    class CloudAnnotationRecord {
        +id, objectIds, anchorWorldPos
        +leaderEndWorldPos?, selectionBbox?
        +comments? : AnnotationComment[]
    }
    class RectAnnotationRecord {
        +id, objectIds, obb, anchorWorldPos
        +comments? : AnnotationComment[]
    }
    class ObbAnnotationRecord {
        +id, objectIds, obb, anchor
        +comments? : AnnotationComment[]
    }
    class AnnotationComment {
        +id : string
        +annotationId : string
        +annotationType : text|cloud|rect|obb
        +authorId / authorName / authorRole
        +content / replyToId?
        +createdAt : number
    }
    class AnnotationReviewState {
        +resolutionStatus : open|fixed|wont_fix
        +decisionStatus : pending|agreed|rejected
        +note?, updatedAt?
        +history : AnnotationReviewEvent[]
    }
    class MeasurementRecord {
        <<distance | angle>>
        +id, kind, origin, target, (corner)
        +visible, createdAt
    }
    class ConfirmedRecord {
        +id : string
        +taskId? / formId?
        +type : 'batch'
        +annotations[] / cloudAnnotations[]
        +rectAnnotations[] / obbAnnotations[]?
        +measurements[]
        +confirmedAt, note
    }

    AnnotationRecord "1" --> "*" AnnotationComment : inline
    CloudAnnotationRecord "1" --> "*" AnnotationComment : inline
    RectAnnotationRecord "1" --> "*" AnnotationComment : inline
    ObbAnnotationRecord "1" --> "*" AnnotationComment : inline
    AnnotationRecord --> AnnotationReviewState
    ConfirmedRecord "1" --> "*" AnnotationRecord
    ConfirmedRecord "1" --> "*" CloudAnnotationRecord
    ConfirmedRecord "1" --> "*" RectAnnotationRecord
    ConfirmedRecord "1" --> "*" ObbAnnotationRecord
    ConfirmedRecord "1" --> "*" MeasurementRecord
```

### 4.2 后端持久化字段

| 表 | 字段（关键） |
|---|---|
| `review_tasks` | id / form_id / current_node / status / requester_id / checker_id / reviewer_id / approver_id / workflow_history (snapshot) / review_comment / created_at / updated_at |
| `review_records` | id / task_id / form_id / type / annotations / cloud_annotations / rect_annotations / obb_annotations / measurements / current_node / operator_id / operator_name / slot_key / snapshot_hash / confirmed_at / note |
| `review_comments` | id / annotation_id / annotation_type / author_id / author_name / author_role / content / reply_to_id / created_at |
| `review_workflow_history` | id / task_id / from_node / to_node / action / actor_id / actor_name / comment / timestamp |
| `review_form_model` / `review_attachment` | workflow/sync 聚合用，含 form_id 维度模型与附件清单 |

> **注意**：`review_records.annotations[i]` 内嵌 inline `comments` 字段；`review_comments` 表本身又是评论的真源——存在双源结构（本审计的核心问题，见第 8 节）。

## 5. 批注 CRUD 时序

### 5.1 在 ReviewPanel 内创建文字/云线/矩形批注

```mermaid
sequenceDiagram
    participant U as 用户
    participant RP as ReviewPanel
    participant TS as useToolStore
    participant V as Viewer

    U->>RP: 点击工具按钮（text / cloud / rect / measure）
    RP->>V: viewerTools.activate(toolKind)
    U->>V: 在场景中点选 / 框选 / 标记
    V->>TS: addAnnotation / addCloudAnnotation / addRectAnnotation /<br/>addObbAnnotation / addMeasurement
    TS->>TS: 本地持久化 plant3d-web-tools-v5:scope
    TS-->>RP: 列表更新
```

### 5.2 批注评论（DUAL_READ 接入后）

```mermaid
sequenceDiagram
    participant U as 用户
    participant CT as ReviewCommentsTimeline
    participant API as plant-model-gen
    participant TS as useToolStore
    participant Store as commentThreadStore  «M3 新增»
    participant Log as commentEventLog

    Note over CT: onMounted / props.annotationId 变化
    CT->>API: GET /comments/by-annotation/{annotationId}?type=
    API-->>CT: AnnotationComment[]
    CT->>TS: setAnnotationComments(type, id, list)

    U->>CT: 提交评论
    CT->>API: POST /comments
    API-->>CT: { comment }
    CT->>TS: addCommentToAnnotation(type, id, comment)

    Note over CT,Store: allComments computed 变化触发 watch
    CT->>Store: syncInlineToStore(type, id, list,<br/>{ taskId, workflowNode })
    Store->>Log: dual_read_diff (差异时)
```

### 5.3 评论实时收敛（**当前缺位**）

```mermaid
sequenceDiagram
    participant Other as 其他用户
    participant API as plant-model-gen
    participant WS as WebSocket
    participant RS as useReviewStore
    participant CB as commentAddedCallbacks
    participant CT as ReviewCommentsTimeline

    Other->>API: POST /comments
    API->>WS: broadcast comment_added
    WS->>RS: ws.onmessage
    RS->>CB: 调用所有注册的 callback
    Note over CB,CT: 当前无组件注册 callback<br/>=> UI 不实时更新，<br/>需要刷新或重选 annotation
```

## 6. 确认与回放

### 6.1 确认（confirmCurrentData）

```mermaid
sequenceDiagram
    participant U as 用户
    participant RP as ReviewPanel
    participant Builder as buildReviewConfirmSnapshotPayload
    participant RS as useReviewStore
    participant API as plant-model-gen
    participant TS as useToolStore
    participant Restore as confirmedRecordsRestore

    U->>RP: 点击「确认」
    RP->>Builder: 收集 draft 批注 + 测量
    Builder-->>RP: ConfirmedRecord payload
    RP->>RS: addConfirmedRecord(record)
    RS->>API: POST /api/review/records
    API-->>RS: 持久化 record-id
    RP->>TS: clearAll()
    Note over RS,Restore: confirmedRecords 变化触发<br/>currentTaskRecords computed
    Restore->>TS: importJSON(buildReplayPayload(records))
    TS->>Viewer: syncFromStore()
```

### 6.2 任务切换 / 重进时的恢复

`confirmedRecordsRestore.createConfirmedRecordsRestorer` 监听 `currentTaskId + currentTaskRecords + viewer ready`，hash 化为 `restoreKey`，相同时不重复 restore；不同时：

- 空任务（`taskId=null` 或 `records=[]`）→ `clearAll`（受 `skipClearOnEmpty` 控制）
- 非空 → `importJSON(buildReplayPayload)` + `syncFromStore`
- M2 SHADOW 在此处插桩（默认关）；M3 thread store 在此处 `mergeFromSnapshot`（flag 控制）

## 7. 工作流流转时序

```mermaid
sequenceDiagram
    participant U as 当前节点负责人
    participant FE as ReviewPanel
    participant API as plant-model-gen
    participant DB as SurrealDB

    U->>FE: 点击「提交到下一节点」
    FE->>API: POST /tasks/{id}/submit
    API->>API: 权限校验 (sj→requester_id 等)
    API->>DB: UPDATE review_tasks<br/>current_node=next, status, updated_at
    API->>DB: CREATE review_workflow_history
    API->>API: 若 form_id 存在<br/>sync_review_form_with_task_status
    API-->>FE: ActionResponse

    Note over U,API: 退回路径
    U->>FE: 点击「打回」+ 选择目标节点
    FE->>API: POST /tasks/{id}/return<br/>{ targetNode, comment }
    API->>API: can_return_to(current, target)
    API->>DB: UPDATE current_node=target,<br/>status=draft (或类似)
    API->>DB: CREATE review_workflow_history (action=return)
```

## 8. 审核结论

### 8.1 优点

- **职责拆分清晰**：`useToolStore` 专管 draft，`useReviewStore` 专管 task 状态，`reviewApi` 专管 HTTP，`reviewerTaskListActions` 专管选择动作。
- **平台内 + PMS 嵌入**双入口共用同一 viewer 渲染管线（`importJSON → syncFromStore`），降低视图层不一致风险。
- **后端工作流权限按节点细分**（`has_permission` 矩阵），与角色映射一致。
- **回放 hash 去重**（`buildSceneKey`）避免 viewer 重复刷新。

### 8.2 问题（与 M2/M3/M4 重构对应）

| 现状问题 | 已开启的对策 |
|---|---|
| `review_records.annotations[i].comments` 与 `review_comments` 表**双源**，replay/restore 时需重新挂载（`mergeWorkflowCommentsIntoRecords`） | M3 commentThreadStore 解耦（已落地 T1~T7） |
| 评论 inline 字段使 annotation 结构越来越重，导出/导入/合并都要承担 merge 逻辑 | M3 thread store 成为唯一真源（T8 CUTOVER 计划就绪） |
| 平台内（taskId）与外部嵌入（formId）走两套 restore 入口，恢复结果可能不一致 | M2 ReviewSnapshot 中间层 + 两个 adapter（已落地，SHADOW flag 默认关） |
| `comment_added` WebSocket 事件**无组件订阅**（`commentAddedCallbacks` 在前端被搜索 0 命中），评论实时性事实上失效 | M5 commentSyncService 计划就绪 |
| 缺少 `annotationKey` 业务键，annotation 跨快照/跨同步重建后归并依赖 `annotationId`，存在漂移 | M1 v1 已落地；M4 v2 后端权威键计划就绪 |
| 缺少 `review_round`、`workflow_node`（与 `current_node` 区分）字段，无法做"仅本轮 / 累计全部"过滤与节点级归档 | M4 后端 metadata 扩展计划就绪 |
| `useToolStore` 持久化 scope 只到 `project + db`，多任务草稿串扰 | M6 task-scoped draft 隔离计划就绪 |
| 确认成功后 `clearAll() + replay` 硬切，draft 与 confirmed 没有分层 | M7 双层渲染计划（最终阶段，未启动） |

### 8.3 风险点（与重构无关、纯运营层面）

- **WebSocket 鉴权过期** 后只重连不刷 token，重连失败时只能等待用户刷新页面
- **review_workflow_history** 表无清理策略，长期任务历史会持续增长
- **PMS 嵌入** 路径 token 校验失败时 fallback 行为未在 UI 上明确告知用户
- **任务取消** 路径只更新状态，不清理 confirmed records，删除策略不一致

## 9. 关键代码索引

| 主题 | 文件 |
|---|---|
| 任务流转后端 | `plant-model-gen/src/web_api/review_api.rs`（routes `:1014~`、`submit_to_next_node :2691`、`return_to_node :2910`、`get_next_node :2670`） |
| 嵌入聚合后端 | `plant-model-gen/src/web_api/platform_api/workflow_sync.rs`（`sync_workflow_handler :130`） |
| 前端任务列表动作 | `plant3d-web/src/components/review/reviewerTaskListActions.ts` |
| 前端任务态 | `plant3d-web/src/composables/useReviewStore.ts`（WebSocket `:374~487`） |
| 前端工具态 | `plant3d-web/src/composables/useToolStore.ts`（scope `:370`、setAnnotationComments `:1359` 等） |
| 前端确认 / 回放 | `plant3d-web/src/components/review/reviewPanelActions.ts` + `reviewRecordReplay.ts` + `confirmedRecordsRestore.ts` |
| 前端嵌入恢复 | `plant3d-web/src/components/review/embedFormSnapshotRestore.ts` + `embedRoleLanding.ts` |
| 前端评论 UI | `plant3d-web/src/components/review/ReviewCommentsTimeline.vue` |
| M2 ReviewSnapshot | `plant3d-web/src/review/{domain,adapters,services}/*` |
| M3 thread store | `plant3d-web/src/review/services/commentThread*` |

---

本文件聚焦"现状描述 + 审核结论"，重构路径请参见 `批注体系重构-M*执行清单-2026-04-19.md` 系列。
